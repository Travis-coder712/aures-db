# AURES Intelligence Layer — Enhancement Plan

**Living document — updated each session as items are completed or refined.**

Last updated: 2026-04-17 (v2.15.1 baseline)

---

## Why This Document Exists

The data underneath AURES has been transformed (419 supplier records, 1,365 timeline events, 123k BESS bids, 225 operating projects with monthly performance, developer/contractor/OEM/offtaker profiles). The current intelligence layer was built for a much thinner dataset. This plan captures:

1. **Foundation work** (cross-cutting UX plumbing) — must come before new features
2. **Navigation reorganisation** — grouping the hamburger menu
3. **Tier 1/2/3 feature builds** — new intelligence products leveraging enriched data
4. **Completed / in-progress** — so we can pick up across sessions

---

## 0. Foundation Work — Build These First

These are cross-cutting concerns raised in the audit. **Every new feature after this should use these shared pieces**, so building them first saves re-work later.

### F1. ChartFrame — reliable chart rendering
**Problem:** charts across the intelligence layer occasionally don't render, typically because:
- `ResponsiveContainer` lacks an explicit `height` and the parent has no height
- Chart is inside a tab that was `display:none` on first mount → measured 0×0
- Chart is inside a collapsible/expandable section that renders children lazily

**Solution:** a shared wrapper component at `frontend/src/components/common/ChartFrame.tsx`:

```tsx
<ChartFrame title="Foo" height={320} data={rows} csvColumns={[...]}>
  <ResponsiveContainer width="100%" height="100%">
    <BarChart data={rows}>...</BarChart>
  </ResponsiveContainer>
</ChartFrame>
```

Behaviour:
- Enforces explicit `height` (required prop — no more missing heights)
- Wraps existing `ChartWrapper` (keeps PNG + CSV export)
- Uses `IntersectionObserver` + `ResizeObserver` to re-render Recharts when the frame first becomes visible (fixes tab / collapse issues)
- Renders a skeleton while `data` is loading
- Renders a "No data" empty state when `data` is empty
- Accepts `onClickDatum` pass-through to support drill-down (see F2)

**Migration:** replace every existing Recharts block in `pages/intelligence/*` and `pages/BESSCapex.tsx`, `Performance.tsx`, etc.

### F2. Drill-down pattern — click a chart, see what's in it
**Problem:** right now charts show aggregates but you can't drill into the underlying records. The user wants: click a bar → see the projects in that bar → click another column → see their suppliers → back → back.

**Solution:** two pieces of shared plumbing:

1. **`useDrillDown<T>()` hook** — manages a stack of drill states
   ```tsx
   const drill = useDrillDown<{ state: string; year?: number }>()
   // drill.current, drill.push({state:'NSW'}), drill.pop(), drill.reset()
   ```

2. **`<DrillBreadcrumb>` component** — renders the current drill path with clickable segments and "All" at the root.

3. **`<DrillPanel>` slide-in** — when a user clicks a datum, a right-side panel slides in showing the underlying project list (sortable DataTable) without losing the aggregate view. Alternative: inline expand below the chart.

**Recharts wiring:** `<Bar onClick={(datum) => drill.push({state: datum.state})}>`. Same for `<Pie>`, `<Scatter>`.

**Migration target:** start with Performance league tables (user's specific ask), then roll out to all intelligence pages.

### F4. DataProvenance — source, freshness, update triggers
**Problem:** data in the performance and intelligence layers now comes from multiple sources with different refresh cadences — OpenElectricity API (AEMO dispatch), NEMWEB (BESS bids), per-project enrichment (EIS PDFs, WattClarity articles, RenewEconomy / PV Magazine / Energy Storage News, ASX disclosures, developer websites, media releases). Users can't tell where a given number came from, how stale it is, or whether they should trigger a refresh.

**Solution:** a shared `<DataProvenance>` component that every chart/view uses to surface:

```tsx
<DataProvenance
  sources={[
    { id: 'openelectricity', label: 'OpenElectricity', lastUpdated: '2026-04-12',
      staleAfterDays: 3, refreshCommand: 'python pipeline/fetch_openelectricity.py' },
    { id: 'nemweb-bids', label: 'NEMWEB Bids', lastUpdated: '2026-04-15',
      staleAfterDays: 1, refreshCommand: 'python pipeline/fetch_nemweb_bids.py' },
  ]}
/>
```

Behaviour per source:
- **Badge showing "Last updated X days ago"** with green/amber/red traffic light based on `staleAfterDays`
- **Hover tooltip** with full datetime and source description
- **"Update now" button per source** — two modes (phased rollout):
  - **Phase 1 (no backend):** clicking copies the CLI command to clipboard and shows a toast "Run this locally to refresh"
  - **Phase 2 (pipeline trigger endpoint):** POSTs to a new local/internal pipeline API that runs the job and streams status back to the UI

Source registry (initial set):
| Source | Purpose | Staleness threshold |
|---|---|---|
| `openelectricity` | Monthly CF%, revenue, price, curtailment (AEMO dispatch) | 7 days |
| `nemweb-bids` | BESS daily bids (10 price bands × direction) | 1 day |
| `aemo-gen-info` | AEMO Generator Info registration | 30 days |
| `epbc-referrals` | Environmental referrals matched to projects | 30 days |
| `rez-isp` | AEMO ISP REZ data | 180 days |
| `news-rss` | RSS from RenewEconomy / PV Magazine / Energy Storage News | 1 day |
| `wattclarity` | WattClarity article scrape | 7 days |
| `asx-disclosures` | Listed developer ASX announcements | 7 days |
| `web-research-oems` | OEM supplier research (solar/hydro/wind/bess) | 90 days — mostly one-off |
| `web-research-offtakes` | PPA counterparty research | 90 days — mostly one-off |
| `eis-pdfs` | Project EIS technical extraction | 180 days |
| `developer-websites` | Developer website scrape | 30 days |

**Placement:** a compact source-chip strip at the top of every chart / analysis section. Also an expanded "Data Sources & Status" page (exists today — refresh it to drive off the same registry) gathering all sources into one admin view with per-source update buttons.

**Prior art in this codebase:** `Performance.tsx` already has an "Update Performance Data" collapsible panel with CLI instructions. DataProvenance generalises that pattern and applies it everywhere.

### F3. DataTable — sortable, totals, row numbers
**Problem:** tables across the app have inconsistent behaviour. The user wants, on every table:
- Sortable by any column (click header, toggle asc/desc)
- Row number in first column (resets when filtering)
- Total row at bottom (sum / avg / count depending on column type)
- Sticky header when scrolling
- Stays within existing `ScrollableTable` for horizontal overflow

**Solution:** a shared `<DataTable>` component at `frontend/src/components/common/DataTable.tsx`:

```tsx
<DataTable
  rows={projects}
  columns={[
    { key: 'name', label: 'Project', sortable: true },
    { key: 'capacity_mw', label: 'MW', format: 'number0', align: 'right',
      sortable: true, aggregator: 'sum' },
    { key: 'capacity_factor_pct', label: 'CF%', format: 'percent1', align: 'right',
      sortable: true, aggregator: 'avg' },
    { key: 'revenue_aud', label: 'Revenue', format: 'currency0', align: 'right',
      sortable: true, aggregator: 'sum' },
  ]}
  showRowNumbers
  showTotals
  defaultSort={{ key: 'capacity_factor_pct', dir: 'desc' }}
  onRowClick={(row) => navigate(`/projects/${row.id}`)}
/>
```

Features:
- Automatic sort indicator (▲ / ▼) on active column
- Row numbers in column 0 — count reflects visible rows after filtering
- Aggregator options: `'sum' | 'avg' | 'count' | 'median' | (rows) => string`
- Format options: `'number0' | 'number1' | 'percent1' | 'currency0' | 'date'` + custom
- CSV export hook (reuses existing `exportDataAsCSV`)
- Optional `onRowClick` for drill-through
- Integrates with `<ScrollableTable>` for wide tables

**Migration:** replace all `<table>` usages across intelligence pages. This is a ~200-line component that will be used 40+ times.

**Alternative considered:** TanStack Table v8 — industry standard, feature-rich. Rejected for now because: (a) heavier dependency, (b) we want the component to enforce consistent dark-theme styling, (c) our needs are modest (sort + totals + row numbers).

---

## 1. Navigation Reorganisation

Replace the flat 17-item hamburger menu with 6 grouped sections.

```
━━━━━━━━━━ EXPLORE ━━━━━━━━━━
  Home
  Dashboard
  Projects
  Map
  Search
  Watchlist

━━━━━━━━━━ INTELLIGENCE ━━━━━━━━━━
▸ Performance & Revenue           (NEW HUB)
    · Fleet League Tables         (← /performance)
    · Revenue Intelligence        (← /intelligence/revenue)
    · BESS Bidding Strategy       (← /intelligence/bess-bidding)
    · Generation & Dispatch       (← /intelligence/energy-mix, Generation Stack tab)

▸ Equipment & Technology          (NEW HUB)
    · OEM Intelligence            (← /oems, significantly upgraded)
    · BESS Capex Analysis         (← /analytics/bess-capex)
    · EIS Technical Specs         (← /intelligence/eis-technical)
    · Wind & Solar Resource       (← /intelligence/wind-resource + new solar equivalent)

▸ Developers & Contractors        (NEW HUB)
    · Developer Intelligence      (← /developers, merged with DeveloperScores)
    · Contractor Intelligence     (← /contractors, expanded)
    · Offtakers & PPAs            (← /offtakers, expanded)

▸ Pipeline & Delivery             (NEW HUB)
    · Scheme Tracker (CIS/LTESA)  (← /intelligence/scheme-tracker)
    · Drift Analysis              (← /intelligence/drift-analysis)
    · Project Timeline            (← /analytics/project-timeline)
    · NEM Activities              (← /intelligence/nem-activities)

▸ Grid & Geography                (NEW HUB)
    · Renewable Energy Zones      (← /rez, expanded)
    · Transmission & Grid         (merge /intelligence/grid-connection + /intelligence/transmission-infra)
    · Dunkelflaute Monitor        (← /intelligence/dunkelflaute)
    · Energy Mix & Coal Watch     (← /intelligence/energy-mix, other tabs)

━━━━━━━━━━ RESOURCES ━━━━━━━━━━
  News
  Guides
  Data Sources
```

### Duplicates to merge / retire
- `/intelligence/grid-connection` → merge into `/intelligence/transmission-infra` (the latter has the Leaflet map and is strictly richer)
- `/intelligence/battery-watch` → **MERGE, DO NOT RETIRE BLINDLY**. The standalone BatteryWatch component is 1,080 lines and contains unique content the user relies on (NSW/QLD BESS buildout, capacity milestones, demand context, coal displacement timeline). Before retiring the route: (a) diff the standalone component against the CapacityWatch.tsx (1,438 lines) that sits inside Energy Mix; (b) port any unique sections into CapacityWatch; (c) consider renaming CapacityWatch → a name that reflects the combined scope (eg "DeploymentWatch" or keep "CapacityWatch" and add a BESS-focused sub-section). Only then retire the standalone route.
- `/intelligence/wind-resource` → becomes "Wind & Solar Resource" (add solar equivalent)

---

## 2. Tier 1 — Transformative (build these first)

### T1.A  OEM Intelligence — Equipment Deep Dive by Type
**Status:** planned

**Current state:** `/oems` is a flat directory of 86 OEMs. No equipment-type deep dive, no market-share analysis, no performance correlation.

**Prerequisite:** rebuild `oem-profiles.json` exporter to include `solar_oem` role (currently 0 solar OEMs in profiles — role was added to DB constraint but exporter wasn't updated).

**New structure — tabbed by equipment type:**

| Tab | Content |
|---|---|
| **Wind OEMs** (77) | Market share (MW, projects) · turbine model library · rotor/rated capacity evolution over COD years · state concentration · contractor pairings · avg installed-fleet CF per OEM |
| **Solar OEMs** (51 panel + 56 inverter) | Panel vs inverter split · market share · SMA/Ingeteam inverter dominance · First Solar/Canadian/Jinko leadership · tracker vs fixed mount if known from EIS |
| **BESS OEMs** (57) | Market share · chemistry breakdown (LFP vs NMC) · combined OEM+inverter roles (Tesla/Fluence/Wartsila) · $/kWh trends per OEM (joined to BESS Capex) · bidding behaviour per OEM fleet (joined to BessBidding) |
| **Hydro OEMs** (42) | Fuji Tasmania monopoly · Toshiba/Voith/Boving comparisons · refurbishment vs new-build |
| **Cross-cutting** | "Who supplies whom" OEM × developer heatmap · OEM → performance quartile correlation · OEM pipeline visibility (upcoming wins in dev/construction) |

### T1.B  Developer Intelligence — Portfolio Explorer
**Status:** planned

**Current state:** `/developers/:slug` shows a basic stat card, one bar chart, and a project list. It does not answer "what is Edify actually doing right now?"

**Target:** turn DeveloperDetail into a portfolio dashboard:
- **Portfolio Gantt** — every project the developer owns, lifecycle stages (conception → dev → construction → operating), original vs actual COD deltas
- **Pipeline waterfall** — counts by stage, click-through to filtered project list
- **Execution scorecard** — on-time %, avg drift, completion rate, grade (fold in DeveloperScores)
- **Technology specialisation** — project-size distribution, hybrid patterns, typical duration ratios for BESS+solar
- **Equipment preferences** — go-to OEMs/contractors (from supplier data): "Edify uses Tesla on 80% of BESS"
- **Offtake strategy** — counterparties, typical PPA tenor, merchant vs contracted %
- **Ownership history** — when have they bought/sold projects?
- **Operating fleet performance** — aggregate quartile distribution
- **Revenue profile** — aggregate Rev/MW vs fleet median

Plus on DeveloperList:
- "Compare developers" feature (pick 2–4 side-by-side)
- Market concentration / Herfindahl by tech

### T1.C  Lifecycle Quartile Matrix
**Status:** planned

A single high-level grid — **asset type × state × lifecycle stage** — showing project counts and quartile-like rankings at every intersection.

Different quartile metrics per stage:
- **Operating** → composite performance (CF × rev × curtailment) — league table quartile exists
- **Construction** → delivery quartile (expected drift × developer score × capex efficiency)
- **Development** → probability-of-delivery quartile (dev score × CIS win × EIS stage × REZ access × grid status)

Click any cell → drill into filtered project list. This becomes the "state of the nation" view.

Cross-cutting: uses **F2 (drill-down)** and **F3 (DataTable)** extensively.

---

## 3. Tier 2 — High Impact

### T2.D  Contractor Intelligence
- Contractor × developer heatmap
- Contractor × OEM preferred pairings
- Delivery track record (avg COD drift for projects they've delivered)
- Capacity pipeline — what's currently under construction, when it frees up
- Technology specialisation (CPP = BESS-heavy, GE = EPC, etc.)
- State coverage map

### T2.E  Offtaker Intelligence — PPA Market Mapper
- Market share by PPA volume — who's buying
- Buyer profiles — Snowy vs AGL vs Origin vs corporates (Telstra, BHP, Woolies) vs retail
- Offtake type breakdown — PPA vs VPPA vs sleeved vs swaps vs LGC-only
- Avg PPA tenor by buyer type
- Developer × offtaker matrix — who signs with whom
- Uncontracted fleet risk — operating projects with no known offtake, cross-referenced to Revenue Pressure

### T2.F  Solar Resource Analysis
Mirror the existing Wind Resource page:
- State GHI/DNI benchmarks
- CF rankings (solar fleet median ~25%, top-quartile ~29%)
- Tracker vs fixed-mount performance gap
- Tilt/azimuth from EIS where available
- Dev pipeline CF predictions
- Curtailment risk scoring (rooftop PV penetration × state)

### T2.G  BESS Portfolio Intelligence
- BESS duration explorer — 2h → 4h → 8h transition timeline
- Grid-forming vs grid-following tracker
- Co-located vs standalone split
- Chemistry lineage (LFP dominance)
- Virtual transmission / SIPS / network service contracts (Waratah, Wallgrove, Torrens)

---

## 4. Tier 3 — Nice-to-Have

- **T3.H** Equipment Lifecycle & Refurbishment Tracker — ageing wind fleets approaching 20 years
- **T3.I** Supply Chain Concentration Risk — Herfindahl by equipment type, single-OEM dependency flagging
- **T3.J** Scheme Win Probability Model — predict CIS/LTESA win probability for development-stage projects
- **T3.K** Repowering Candidates Dashboard — operating wind >15 years, CF trend, refurb opportunity

---

## 4b. Guides & Documentation — Complete Rewrite

**Status:** planned

**Current state:** 12 guides in `frontend/src/data/guides.ts` (~2,066 lines). Many are stale artefacts from earlier phases:

| ID | Title | Status |
|---|---|---|
| `plain-english-overview` | What Is AURES? | Keep — refresh for new intelligence layer |
| `project-plan` | Architecture & Plan | Out of date (pre-enrichment) — rewrite |
| `build-tracker` | Build Progress | Stale changelog — retire (git log does this) |
| `session-guide` | Session Guide | Meta/process doc — retire or move to dev-only |
| `vibecoding-notes` | How This Was Built | Retrospective — retire or move to dev-only |
| `interactive-map` | Interactive Map (Planned) | Map is built — retire, or rewrite as a usage guide |
| `nem-summary` | NEM Summary Dashboard (Planned) | Dashboard is built — retire or rewrite |
| `using-aures` | Using AURES on Your Phone | Keep — refresh |
| `performance-methodology` | Performance Metrics Deep Dive | Keep — refresh with quartile logic |
| `strategic-roadmap` | Strategic Roadmap | Stale — replace with a roadmap derived from this plan |
| `search-tips` | Search Tips | Keep |
| `data-quality` | Data Quality Audit | Keep — refresh with current coverage numbers |

### Proposed new guide set (consolidated, ~6–8 guides)

| Category | Guide | Content |
|---|---|---|
| **About** | What Is AURES? | Plain-English explainer, refreshed for current state |
| **About** | Data Sources & Quality | Merge `data-quality` + freshness info · what's covered · gaps · confidence tiers |
| **Using** | Finding What You Want | Merge `using-aures` + `search-tips` · Cmd+K · filters · watchlist · mobile |
| **Using** | Navigating the Intelligence Layer | NEW — orient users around the 5 intelligence hubs, when to use each |
| **Methodology** | Performance & Quartile Rankings | Refreshed `performance-methodology` · composite scoring · quartile logic · click-through drill-down |
| **Methodology** | Developer Execution Scoring | NEW — explain how scores are computed, what they mean, limitations |
| **Methodology** | OEM & Supplier Data | NEW — what counts as an OEM vs EPC vs BOP, data sourcing, confidence |
| **Roadmap** | Where This Is Going | Replace `strategic-roadmap` with a user-facing summary of this plan |

### Principles for the rewrite
- **Keep user-facing; retire process docs.** `build-tracker`, `session-guide`, `vibecoding-notes` should not live in user guides. Move them to `docs/` as dev notes if kept at all.
- **Every guide must cite current features**, not "Planned — coming soon" placeholders.
- **Link between guides** — each methodology guide should link from the analysis page that uses it (eg Performance page has "Read: Performance & Quartile Rankings").
- **Include screenshots or interactive callouts** if practical (currently Markdown-only).
- **One revision pass per major release** — when v2.18.0 ships OEM Intelligence, the OEM guide gets updated in the same commit.

### Build order
This sits as a parallel track to the intelligence layer work. Rewrite each guide when its corresponding feature ships — eg OEM guide rewrite lands with T1.A (OEM Intelligence). The "About / Using / Navigating" guides can be rewritten any time after F1/F3/nav reorg complete.

---

## 5. Quick Wins (batch whenever convenient)

1. Rebuild `oem-profiles.json` with `solar_oem` role (prereq for T1.A)
2. Merge GridConnection into TransmissionInfra
3. Retire standalone Battery Watch route
4. Move `/analytics/bess-capex` and `/analytics/project-timeline` under `/intelligence/*`
5. Group hamburger menu into 6 sections (layout-only change)

---

## 6. Recommended Build Order

| Session | Work | Output |
|---|---|---|
| 1 | Foundation F1 (ChartFrame), F3 (DataTable) · regenerate solar OEM profile · quick wins 2–5 · group hamburger menu · retire stale guides | v2.16.0 |
| 2 | Foundation F4 (DataProvenance — Phase 1 clipboard-copy mode) · surface on Performance + Intelligence pages | v2.17.0 |
| 3 | Foundation F2 (drill-down pattern) · retrofit Performance charts with click-through · rewrite About/Using/Navigating guides | v2.18.0 |
| 4 | T1.A — OEM Intelligence tabbed deep-dive · rewrite OEM & Supplier Data guide | v2.19.0 |
| 5 | T1.B — Developer portfolio dashboard · rewrite Developer Execution Scoring guide | v2.20.0 |
| 6 | **T2.E — Offtaker Intelligence / PPA Market Mapper** (promoted from Tier 2 ahead of T1.C — offtakes need their own first-class home, not a sub-section of DeveloperDetail) | v2.21.0 |
| 7 | T1.C — Lifecycle Quartile Matrix · rewrite Performance & Quartile Rankings guide | v2.22.0 |
| 8+ | Remaining Tier 2 features in user-priority order · F4 Phase 2 (pipeline trigger endpoint) · remaining guide rewrites | v2.23+ |

---

## 7. History of Development → GitHub is the Source of Truth

**Decision:** the history of this project lives in **git commits, tags, and GitHub releases** — not in markdown files. The `build-tracker` guide will be retired for this reason.

**How we use it:**
- **Every user-visible change ships with a version bump** in `frontend/package.json` (eg v2.15.1 → v2.16.0).
- **Every release gets a GitHub Release** (`gh release create v2.x.y`) with a short summary pulled from the commit trail — this gives a permanent, human-readable changelog accessible from github.com/Travis-coder712/aures-db/releases.
- **Commits are written for humans** — short why-centric message, not "wip / fix typo".
- **This plan document lives in `docs/`** and is committed alongside code changes, so the plan's own evolution is also tracked in git.
- **A future "Release History" page** in the app (under Resources or a footer link) can simply render the GitHub Releases feed via its public JSON API — no duplicate maintenance.

### Current baseline: v2.15.1 (committed 2026-04-17)

Recent notable releases (from git log):
- **v3.21.2** — **CIS T9 Competitive Field Deep Dive section** added to the CIS T9 OpenRound card (`t9CompetitiveFieldDeepDive` field on `OpenRound` type + `T9CompetitiveFieldDeepDive` React component). Renders in the Open Rounds CIS tab under the CIS T9 card, after ProformaMechanicsDeepDive. Structure: (1) exec-summary headline; (2) state-math grid (VIC ≥1.6 GW min, TAS ≥300 MW min, 470 MW VIC solar cap, 3.1 GW contestable pool, 500 MW FN set-aside, target COD <2030); (3) deliverability paradox frame + Zero-for-15 sidebar (2 of 69 non-battery CIS awards had reached construction by March 2026; Palmer/Tilt was first) + withdraw-and-resubmit clarification; (4) three collapsible-card lenses — Rebid (purple), Gentailer (blue), Earliest-COD (green) — each with top-5 picks + flag chips (STANDOUT / WATCH / LONG-SHOT / BLOCKED) + lens conclusion; (5) Combined-view predicted T9 shape table (7 pools totalling ~5 GW) + bottom-line; (6) Standout pitch callout box in emerald — AGL Barn Hill Wind + BESS with 7 signals + reasoning; (7) Docs reference footer pointing to `docs/RESEARCH_CIS_T9_COMPETITIVE_FIELD.md` for the full ~6,700-word analysis. Complements the docs note by surfacing the scannable synthesis in the app, mirrors the NSW T8 deep-dive pattern (RiskShare / PpaLeverage / BidParamsOptions / BidConfig / ProformaMechanics).
- **v3.21.1** — **CIS Tender 9 competitive-field research note + AURES data corrections.** New docs/RESEARCH_CIS_T9_COMPETITIVE_FIELD.md (~6,700 words) covering: T9 opportunity + corrected state math (VIC/TAS numbers are MINIMUMS not caps; contestable ~3.1 GW open to all eligible states above minimums); deliverability paradox as central frame (Zero-for-15 problem — only 2 non-battery CIS generators had reached construction of ~69 awards by Mar 2026); Table A of 35 non-NSW CIS-locked projects (T1/T4/T7) curated from AURES scheme_contracts overlays; Table B tiered T9 competitive field (wind + large solar + solar+BESS hybrids) with Tier 1/2/3/Excluded classification; state-by-state summary; developer deliverability signals table; **three-lens synthesis section**: (Lens 1) Prior-CIS rebid candidates via active withdraw-and-resubmit pathway — Kentbruck/HMC most likely; (Lens 2) Gentailer + path-to-market — **AGL Barn Hill Wind + BESS (SA, 300 MW wind + 270 MW BESS, AGL-owned since 2009, EPBC-approved) is the standout gentailer pitch**; (Lens 3) Earliest COD — QLD mid-scale solar+BESS dominant (Fraser Coast, Northern Midlands, Solar River SB, Colosseum, Mount Hopeful). Combined-view prediction: T9 will look like T7 (~2-3 GW mid-scale QLD + ~1 GW proven-delivery + ~500 MW FN set-aside + ~1.5-2 GW VIC min + ~300-500 MW TAS min + ~1-3 gentailer wins). Executive-summary callout box at top of doc. **AURES data corrections bundled**: Bell Bay overlay (T4 scheme_contract added — was missing); Whyte Yarcowie overlay (proponent WP Renewables → EDF Renewables Australia; ownership chain noted); Palmer overlay + DB (status development→construction + FID 9 Jan 2026 timeline entry — first non-battery CIS generator to reach construction); **Barn Hill overlay + DB (capacity 186→300 MW; owner corrected to AGL Energy since 2009 — was showing SPV name only)**; scheme-rounds.ts CIS T9 entry `whatChanged` region row rephrased (min-not-cap correction). Mega-exporter re-run; all 40+ frontend JSONs regenerated. Two versions synced at 3.21.1.
- **v3.18.0–v3.18.8** — **CIS Pipeline Intelligence overhaul.** CIS T8 dispatchable results (15 BESS, 4.2 GW / 16.1 GWh). `contract_status` field added to SchemeProject — all 130 CIS + LTESA projects verified via deep web research (58 awarded, 15 FID, 21 construction, 7 operating). CIS Pipeline Overview on SchemeTracker Overview tab: Pipeline Funnel (awarded→signed→FID→construction→operating), By Round status bars with click-to-drill-down project lists, Project Table with filter pills. Scheme status flows to ProjectDetail cards. SchemeTracker tabs reduced 11→9 (removed Boardroom + CIS Success). 18 timeline events added to DB. 30 research notes. Bowen CIS withdrawal clarification added to NSW LTESA T8 eligibility. Capricorn BESS QLD call-in note. project_id mismatch audit gotcha added to CLAUDE.md.
- **v3.17.5** — **Contract Market visualisation** in Battery Market Intelligence. Quarterly spot prices by state (Q1 2024→Q1 2026), $300 cap contract settlement chart, ASX forward curve CY26-CY29 by state, spread analysis, BESS investment hierarchy (SA > NSW > VIC >> QLD). New `nem-contract-prices.json` curated from AEMO QED, AER, ASX Energy, Leading Edge.
- **v3.17.4** — QLD Briefing fixes: PDF captures all sections, Supernode corrected to 260 MW Stage 1, stacked operating/construction chart, event type toggles, 4-tier pipeline breakdown.
- **v3.17.3** — **QLD Battery Investment Briefing** (`/intelligence/qld-battery-briefing`) — opinionated investment memo: thesis, revenue collapse, pipeline timeline, analysis (CIS, Callide, BTM, data centres, scenarios), NEM context.
- **v3.17.2** — AURES Learn Energy Transition corrections: gentailer + basis risk, AGL/Accel/Origin/Alinta/RET factual fixes. NEMweb bids refreshed (Mar→May 2026). Data freshness checklist in SESSION_OPENER.
- **v3.17.0-v3.17.1** — **Battery Market Intelligence** (9 sections: Overview, Price Setting, Revenue, Bidding, Cannibalisation, Wholesale, Contracts, By State, Outlook) + **Research Notes** (25 notes, 6 categories). Deep research from Modo Energy, WattClarity, AEMO QED, RenewEconomy.
- **v3.16.8** — Full data refresh per `docs/DATA_REFRESH.md` runbook. All 7 importer phases ran with overlay/COALESCE/status-priority guardrails enforced — hand-curated project names + statuses + connection details + scheme contracts preserved across the run (6 spot-check projects confirmed name + status match pre-refresh baseline). **Refresh summary:** (1) News RSS — 70 articles imported (410 total in DB, 385 with project matches). (2) AEMO Gen Info monthly — 479 projects updated, **568 enriched skipped (protection)**, 13 consolidated stages recognised, 1 status-downgrade prevented (`tomago-bess` kept at `construction` — AEMO said `development`); 0 new projects, total still 1,064. (3) AEMO IASR Workbook — 146 entries loaded (90 Committed + 56 Anticipated); top REZ matches Central-West Orana 5 projects / 2,585 MW, South West VIC 9 projects / 1,726 MW. (4) EnergyCo REZ Access Rights — 19 access rights ingested, all matched (0 unmatched), 14 projects updated. (5) Open Electricity 2026 YTD performance — 224 projects refreshed with data through 31 May 2026 (217 facilities with energy data; API quota 499/500 remaining). (6) MMSDM dispatch suite — DISPATCH_REGIONSUM March 2026 (192 daily region rows), DISPATCHPRICE rolling 14d (70 region-rows) + March 2026 monthly (160 region-rows), BESS 5-min rolling 7d (504 records) + March 2026 monthly (1,344 daily-peak records), battery SCADA rolling 7d (16,126 points → 20 battery records / 35 daily-region rows), DISPATCHLOAD coal rolling 7d (76,032 rows). April + May 2026 NOT yet published in MMSDM (AEMO ~45-day lag — next refresh ~mid-July will pull April). (7) EPBC referrals — 170 matched + 394 unmatched referrals stored, 1 project upgraded to `planning_approved`. **Validator pass:** 0 hard mismatches; 8 informational COD-review flags (projects whose stated COD has passed but status still `construction`) — flagged for manual review, NOT auto-fixed: bundaberg-solar-farm, golden-plains-wind (Stage 1), goorambat-east-solar-farm-engie, gunsynd-solar-farm, lancaster-solar-farm, limondale-bess, munna-creek-solar-farm, wambo-wind-farm. **Mega-exporter:** all frontend JSONs regenerated with overlay merges applied. Frontend tsc + build clean. Both version files synced at 3.16.8 (PWA refresh-badge will fire). **Guardrails enforced:** name column never touched by any importer (verified by grep); status-downgrade protection (STATUS_PRIORITY); COALESCE protection on connection_status / rez_access fields; OVERLAY_OVERRIDE_FIELDS at `pipeline/exporters/export_json.py:40` honoured at export.
- **v3.16.7** — **Proforma Contract Mechanics Deep Dive** added to NSW T8 card (`proformaMechanicsDeepDive` field + `ProformaMechanicsDeepDive` component). Second-pass systematic extraction from the gazetted proforma Gen + Hybrid Gen LTESA (18 May 2026 publication) covering the financier-diligence material that the public fact sheets gloss over. Eleven topic cards (collapsible, with topic-level flag chips for GAP / FINANCIER RISK / OPPORTUNITY): (1) **Termination Payment Formula** — Fixed Component (NPV of remaining Min Gen × Fixed Price at 7% p.a. discount) + Variable (lesser of 15% × Fixed Term or 90% × HNP). 7% is fixed accounting discount, not mark-to-market. (2) **Events of Default + Cure Periods** — 8 Operator default categories (5 BD Insolvency to 60 BD misrepresentation) + 2 SFV default categories + PDA cross-default trigger. (3) **Change of Control + Assignment + Financier Step-in** — SFV consent "not unreasonably withheld" with objective capability test; tripartite deed contemplated for lender security (cl 22.4); no pre-approved assignee categories for Operator side. (4) **Force Majeure + Major Casualty** — definition incl Network curtailment/congestion; ≥50% capacity threshold applied separately to Gen vs Storage in Hybrid; Major Casualty 6-month election + 5-year reinstatement deadline. (5) **Market Disruption Events** — 5 triggers, 20 BD negotiation → 60 BD Independent Expert; NO hard backup-pricing methodology (flagged as GAP). (6) **Annual Payment Cap mechanics** — P50 forecast LOCKED at tender, Excess Amount carry-forward within Swap Period, Operator absorbs overperformance. (7) **Capacity Products + Green Products + LGCs** — SFV-nominated Notional Price; LGCs deemed-nominated as Green Product at signing → transferred to SFV; FCAS EXPLICITLY EXCLUDED → retained by Operator (flagged as OPPORTUNITY for storage-heavy Hybrid bids). (8) **Shortfall Sums (Gen) vs Aggregate Rebates (Hybrid)** — different penalty structures, Hybrid consolidates 3 components with Net Swap Payment cap (OPPORTUNITY). (9) **Bid Variable Permitted Ranges** — includes the previously-not-surfaced **Minimum Generation = 75% × forecast P90** rule (downside floor that triggers Shortfall regardless of merchant conditions); Strike/RPT relationship unconstrained in proforma. (10) **Settlement Timing + Default Interest** — 20 BD invoice + 20 BD payment = 40 BD settlement lag; Default Interest = RBA + 2% daily; Termination Payment interest deferred 60 BD. (11) **Change of Law + Relevant Cost Change** — narrow protection: general CoL excludes Fixed Price adjustment; RCC triggers 50% pass-through only above $500k indexed threshold; carbon-policy explicitly absent (flagged as GAP); planning/environmental/native title explicitly excluded from CoL definition. Six cross-topic gaps surfaced separately (P50 audit rights, MDE backup pricing, fixed 7% discount, carbon-policy gap, planning/environmental CoL exclusion, no pre-approved Operator assignees). Render position in OpenRoundCard: after BidConfigDeepDive — so flow is RiskShare → PpaLeverage → BidParamsOptions → BidConfig → ProformaMechanics → productComparison → etc. Designed for financier diligence + settlement modelling reference.
- **v3.16.6** — Systematic proforma sweep of the NSW T8 content using the gazetted Gen + Hybrid Gen LTESA proforma contracts (Tender Round 8 publication version, 18 May 2026). Four open items resolved, one prior-content error corrected: (1) **Nominated Percentage is DISCRETE — 25%, 50%, 75%, 100% only** (Gen LTESA Schedule 1 Exercise Notice template; Hybrid LTESA cl 11.3 explicitly invalidates "a Nominated Percentage in respect of a Swap that is not 25%, 50%, 75% or 100%"). The v3.16.4 BidParamsOptionsDeepDive content speculated continuous 0–100% values "pending proforma confirmation" — that was WRONG (and Travis\'s original framing of "(25, 50)" steps was correct). Updated `optionStructure.partialExercise` prose to surface this discrete constraint. Headline copy updated. The "no exercise" worked example also corrected — non-exercise is not "delivering an Exercise Notice with 0%" (which is invalid per proforma), it\'s simply not delivering an Exercise Notice within the 12→6 month window; the Option lapses. (2) **"Eligible Contract" Repayment-reduction formula CONFIRMED** (Gen LTESA cl 12 / Hybrid LTESA cl 13): Repayment Amount = min(HistoricalNetPayments, (DWFP − RTP) × ContractedPct × (ΣGenTI − ΣLTESATI − ΣECTI)), where ΣECTI is the cumulative Eligible Contract notional volume. Eligible Contract definition = "any contract that has the net effect of reducing the LTES Operator\'s exposure to volatility of the Floating Price" — PPAs, offtake agreements, tolls, hedging contracts all qualify (Related-Entity contracts must be arm\'s length). Eligible Contract\'s hedged volume directly subtracts from the Repayment-exposed volume — a 100% PPA at a marginal interval effectively zeroes the LTESA Repayment for that interval\'s volume. Updated 4 places that previously flagged this as "in proforma" — `caveats[2]`, `designStatus.openItems[0]`, `ppaScenarios.scenarios[0].trap`, and the `ppaScenarios.norQuestion` prose. Removed from `openQuestions`. (3) **Historical Net Payments definition CONFIRMED** (Gen cl 12.2 / Hybrid cl 13.2): HNP = ΣSP − ΣLP = cumulative net flow from SFV to Operator since contract inception, where ΣSP = sum of Net Monthly Amounts SFV paid to Operator, ΣLP = sum of Operator-to-SFV payments (Net Monthly Amounts + Repayment Amounts + Shortfall Sums for Gen, or + Aggregate Rebates for Hybrid). Surfaced in the ppaScenarios trap text — "worst-case is give back what you got, no more". (4) **Gen LTESA negative-spot floor is DOUBLE-FLOORED** in the proforma: at both the Dispatch-Weighted Floating Price level AND the per-interval Trading Interval Amounts level (Item 3.5). Surfaced in the `norQuestion` confirmed-mechanic prose.
- **v3.16.5** — Two changes bundled: (1) **Tab split** — the single "Open Rounds" tab split into **Open Rounds CIS** (federal — T8 storage, T9 NEM-wide gen, T10 storage) and **Open Rounds LTESA** (NSW — T8 gen+hybrid, T9 LDS). Travis's call as the NSW T8 card has accumulated very heavy deep-dive content (RiskShare + PpaLeverage + BidParamsOptions + BidConfig sections; ineligible-projects modal; cashflow calculator) and was visually drowning the CIS rounds it shared the tab with. Refactor details: `OpenRoundsTab` now accepts a `schemeFocus: 'CIS' | 'LTESA'` prop and filters `OPEN_ROUNDS` to that scheme; the in-tab Federal/NSW filter buttons (v3.14.0) are removed since the tab itself is now the filter. Headline copy per scheme — CIS tab says "administered nationally by DCCEEW + ASL" + cross-link to LTESA tab; LTESA tab says "administered by AEMO Services on behalf of the NSW Government" + cross-link to CIS tab. Headline stat row tailored per scheme (Rounds Open Now · Generation Open · Tracked Rounds · Next Bid Deadline — replaces the old "Schemes Active = 2" stat which is now meaningless within a scheme-scoped view). Bid-Close Calendar title now includes the scheme name. Footer-source attribution differs per tab. `TABS` array updated: `'open-rounds'` → `'open-rounds-cis'` + `'open-rounds-ltesa'`. NSW Wind tab cross-reference text updated from "Open Rounds" → "Open Rounds LTESA". (2) **Exercise Notice timing CONFIRMED from gazetted proforma** — Travis supplied the proforma Generation LTESA + Hybrid Generation LTESA contracts (Tender Round 8 publication version, 18 May 2026). The v3.16.4 BidParamsOptionsDeepDive amber-flagged "likely 3-6 months prior" timing was WRONG. Actual mechanic per Gen LTESA clause 10.2(b) and Hybrid Gen LTESA clause 11.2(b): the Exercise Notice may be delivered during a **6-month window opening 12 months before the Swap Start Date and closing on the last Business Day at least 6 months before the Swap Start Date**. So for FY2027/28 Swap Period (1 Jul 2027 → 30 Jun 2028), the window is **1 Jul 2026 → ~31 Dec 2026** — bidders fix annual exercise decisions by 31 December of the prior calendar year at latest. Three confirmed details added: (a) Exercise Notice is **irrevocable once delivered**; (b) Option **auto-exercises on Swap Start Date** without further action; (c) **First Option Date special case** — if COD lands <6 months before a 1 July (or within 12 months after), First Option Date may be delayed up to 18 months unless Operator requests an earlier "Requested Date" at least 8 months prior + delivers Exercise Notice at least 6 months prior. Confidence pill flipped from amber `likely` to green `confirmed`; UI border + label colour updated correspondingly. The LTESA tab footer attribution now explicitly credits the proforma contracts as primary sources.
- **v3.16.4** — TWO new NSW T8 deep-dives shipped together (answering Travis's adjacent strategy questions): (A) **Bid Parameters + Option Mechanics Deep Dive** (`bidParamsOptionsDeepDive` field + `BidParamsOptionsDeepDive` component) answering "what bid variables differ between Gen LTESA vs Hybrid LTESA, and how do the annual options actually work?" Six-row comparison table covers: Strike/Fixed Price (Hybrid uses Strike Price as a swap reference, Gen uses Fixed Price as a one-way floor in max(0, Floating)), Repayment Threshold Price (both products use same 50% PRS repayment formula above RPT — RPT typically set ABOVE Strike/Fixed), Contracted Percentage (bid at tender, fixed for term — % of P50 generation covered), COD Target Date (with prior-round permissible-window precedent — typically 12-24 months on either side), Excluded Periods (specific Swap Periods bid out at tender — typically high-forecast-spot years), Contract Term (Default = 20yr both products; Alternative = variable length — shorter for Hybrid where battery hardware drives 15-yr economic life). Project Parameters by Asset Type lays out the data each of 4 asset types must supply at tender registration. Option Structure section confirms three CONFIRMED facts from the Gen LTESA Fact Sheet Table 1: (1) Swap Period = 1 July → 30 June (Australian financial year); (2) Annual exercise — Operator submits Exercise Notice each year specifying which Swap Period(s) to enable; (3) Partial exercise via Nominated % (sub-100% of Contracted % can be enabled in any year — Swap % = Contracted % × Nominated %). One PROFORMA-DEPENDENT item flagged amber: Exercise Notice timing (likely 3-6 months prior to Swap Period start per prior-round precedent — to be confirmed against proforma). Five partial-exercise worked examples on a 500 MW project: 100%/100% = 500 MW full coverage; 100%/50% = 250 MW half exercise; 100%/25% = 125 MW quarter exercise; PPA-stacked partial; 100%/0% = no exercise (Repayment can still apply above RPT). Concluding note clarifies that Contracted % is locked at tender (fixed for 20-yr term) while Nominated % is the year-by-year flex lever via Exercise Notice. (B) **Bid Configuration Deep Dive** (`bidConfigDeepDive` field + `BidConfigDeepDive` component) answering two specific questions: (1) Is "Assessed Hybrid" the same as "Hybrid Generation LTESA" from an assessment criteria perspective? — NO. "Assessed Hybrid" is a PROJECT CATEGORY (Tender Guidelines Table 1); "Hybrid LTESA" is a CONTRACT PRODUCT. MC1 assessment treatment varies by the COMBINATION — three distinct paths per MC1 Briefing §2.2.1: (a) Non-Assessed Hybrid → Gen LTESA = MC1 assesses gen only; (b) Assessed Hybrid → Gen LTESA = MC1 assesses gen + storage for WMB/SSC/SSS but Net LTESA Cost uses Gen LTESA mechanics ("hybrid benefit, simple cost"); (c) Assessed Hybrid → Hybrid LTESA = MC1 assesses gen + storage AND Net LTESA Cost uses 50% PRS Hybrid mechanics (BCR-maximising path). (2) Can you register as Generation and submit an Alternative Bid as Hybrid? — NO at category level (Project category locked at registration based on physical config); YES at product level for Assessed Hybrid ≥4hr (can bid Gen LTESA Default + Hybrid LTESA Alternative on the same Project). 4 Project Category cards (Generation Only / Non-Assessed Hybrid / Assessed Hybrid <4hr / Assessed Hybrid ≥4hr) with gen-LTESA + hybrid-LTESA eligibility chips per category. 3 assessment-path cards explaining MC1 treatment differences. Registration lock-in callout flags the ~2-week window (22 Jun → 6 Jul 2026) between Project Category confirmation and Bid submission. 3 concrete combination examples. Render order in OpenRoundCard: RiskShareDeepDive → PpaLeverageDeepDive → BidParamsOptionsDeepDive → BidConfigDeepDive → productComparison → settlementProsAndCons → ppaScenarios — conceptual flow: how the 50% works → how PPAs leverage that → what variables you actually bid (and how options work year-by-year) → which bid combos are allowed → product mechanics → trade-offs → PPA scenarios.
- **v3.16.3** _(unreleased — folded into v3.16.4)_ — Bid Configuration Deep Dive was originally cut as v3.16.3 but bundled with v3.16.4 into a single release. (1) Is "Assessed Hybrid" the same as "Hybrid Generation LTESA" from an assessment criteria perspective? — NO. "Assessed Hybrid" is a PROJECT CATEGORY (from Tender Guidelines Table 1); "Hybrid LTESA" is a CONTRACT PRODUCT. MC1 assessment treatment varies by the COMBINATION — three distinct paths per MC1 Briefing §2.2.1: (a) Non-Assessed Hybrid → Gen LTESA = MC1 assesses gen only; (b) Assessed Hybrid → Gen LTESA = MC1 assesses gen + storage for WMB/SSC/SSS but Net LTESA Cost uses Gen LTESA mechanics ("hybrid benefit, simple cost"); (c) Assessed Hybrid → Hybrid LTESA = MC1 assesses gen + storage AND Net LTESA Cost uses 50% PRS Hybrid mechanics (BCR-maximising path). (2) Can you register as Generation and submit an Alternative Bid as Hybrid? — NO at category level (Project category locked at registration based on physical config); YES at product level for Assessed Hybrid ≥4hr (can bid Gen LTESA Default + Hybrid LTESA Alternative on the same Project). 4 Project Category cards (Generation Only / Non-Assessed Hybrid / Assessed Hybrid <4hr / Assessed Hybrid ≥4hr) with gen-LTESA + hybrid-LTESA eligibility chips per category. 3 assessment-path cards explaining MC1 treatment differences. Registration lock-in callout flags the ~2-week window (22 Jun → 6 Jul 2026) between Project Category confirmation and Bid submission. 3 concrete combination examples (500 MW solar + 250/1000 MWh BESS = Assessed Hybrid ≥4hr with both bids; 300 MW wind + 50/100 MWh BESS = Gen LTESA only; 400 MW standalone solar). New `bidConfigDeepDive` field on `OpenRound` type; new `BidConfigDeepDive` component placed between RiskShareDeepDive and PpaLeverageDeepDive — so the conceptual flow is: how the 50% works → which bid combos are allowed → how PPAs leverage that.
- **v3.16.2** — PPA Leverage Deep Dive section added to the NSW T8 card. Plain-English strategy guide answering "does a PPA let you lower the bid parameters, and what are the limits?" Five-lever breakdown: (1) Lower Contracted Percentage (biggest single lever — MC1 Briefing confirms <100% reduces Net LTESA Cost); (2) Lower Fixed/Strike Price (per MC1 Briefing, has greatest single influence on Net LTESA Cost); (3) Higher Repayment Threshold Price (secondary lever); (4) More Excluded Periods (forgo Swap Periods aligned with high-forecast-spot years); (5) Shorter Contract Term (Alternative Bid only — Default Bid term is fixed at 20yr). Each lever explained with how-it-works + MC1-impact + the-limit. Hard limits (rules-based): EC10 LTESA + CISA prohibition, EC3 prior-LTESA award, EC10 financial-support definition incl Commonwealth/State revenue underwriting, PPA contract clauses (offset / change-of-law / revenue-sharing). Soft limits (commercial / scoring): financier DSCR floors, MC1 BCR balance, MC3 deliverability scrutiny, additionality, PPA counterparty consent. Balancing-act summary explains the Goldilocks zone (typically 30-70% PPA volume + Contracted % matching un-PPA'd portion + Fixed/Strike close to un-PPA'd merchant floor + Excluded Periods in highest-forecast-spot years). Worked scenario: same 500 MW wind farm bid two ways — NO PPA (100% Contracted, $75 Fixed, $130 RPT, HIGH Net LTESA Cost) vs WITH 50% PPA @ $90 (50% Contracted, $55 Fixed, $160 RPT, LOW Net LTESA Cost). New `ppaLeverageDeepDive` field on `OpenRound` type; new `PpaLeverageDeepDive` rendering component placed in OpenRoundCard right after RiskShareDeepDive.
- **v3.16.1** — Risk-Share Deep Dive section added to the NSW T8 card. Educational follow-up to v3.16.0 answering Travis's three specific questions: (1) **How does the 50% PRS play out?** — explainer covers exercise periods (50% × (Strike − Floating) swap multiplier) AND non-exercise periods (50% × (Spot − RPT) Repayment multiplier, same percentage in both directions, symmetric design). (2) **Battery charging mechanics** — explainer covers the net-export basis (exports minus imports per trading interval), why charging reduces NotionalQuantity, the AC-coupling vs DC-coupling decision, and why NotionalQuantity = 0 in negative-spot intervals (prevents Operator paying SFV during negative-price events). (3) **Why 50% specifically?** — design rationale: balances project bankability (still meaningful price-floor support) + Operator dispatch incentive (retains 50% of upside to reward efficient battery dispatch) + consumer value-sharing (SFV/customers capture 50% via reduced Net LTESA Cost). Mirror the symmetric 50% used in the Repayment mechanic carried through from prior LTESA rounds. **5 worked examples** at different spot prices ($40 low / $80 mid / $150 high — exercising; $150 NOT exercising; charging interval at $20) with the math broken out — Gen LTESA cashflow $M, Hybrid cashflow $M, realised $/MWh under each product, plus a 1-sentence interpretation per row. **Recharts bar chart** comparing realised $/MWh under Generation LTESA vs Hybrid LTESA (50% PRS) vs spot reference across the three exercise scenarios — makes the "Hybrid tracks halfway between Strike and Spot" intuition visually obvious. New `riskShareDeepDive` field on `OpenRound` type; new `RiskShareDeepDive` rendering component placed between productComparison and settlementProsAndCons in OpenRoundCard.
- **v3.16.0** — Two NSW T8 interactive helpers + Staged-Projects clarification. (1) **NSW-ineligible projects modal** triggered by an info button in the NSW T8 commitmentEligibility callout. Surfaces every NSW project that is affected by the T8 eligibility/merit rules: those that have won a prior CIS or LTESA (EC3/EC10/EC11 — Stage 1 CISA blocks Stage 2 LTESA on the same Project), and those "Operating" in the AURES DB as a proxy for AEMO July 2025 Gen Info "In Service" status (MC1 Wholesale Market Benefits zeroed). NSW-only. Default tech filter = wind (29 projects, 8,136 MW shown); tech chips for All (110) / Wind (29) / Solar (36) / BESS (27) / Hybrid (11) / Pumped Hydro (7). Search box for name filter. Per-row tags: CIS / LTESA / OP. Each project name links to its detail page. Footer caveat clearly flags "Operating" is a proxy. Data derived live at component-load from `CIS_PROJECTS` + `LTESA_PROJECTS` arrays + `projects.status` via `useProjectIndex`. (2) **PPA × LTESA cashflow calculator modal** triggered by a button in the PPA Scenarios section. Models the gazetted T8 settlement formulas (MC1 Briefing Appendix A): Generation LTESA = Σ NQ × (Fixed − max(0,Floating)); Hybrid Generation LTESA = Σ NQ × 50% × (Strike − Floating); Repayment = min(Historical Net Payments, Σ 50% × NQ × (Floating − RPT)) with PPA-as-eligible-contract reduction. Inputs: Product (Gen/Hybrid), Spot, Strike/Fixed, RPT, Contracted %, P50 annual gen, PPA strike, PPA volume %, Exercise (Y/N) — all interactive sliders/toggles with sensible defaults (500 MW wind farm scenario). Outputs: total project revenue ($M/yr + effective $/MWh), per-component breakdown (merchant + PPA + LTESA settlement + Repayment), PPA hedge value, gazetted formula explanations, limitations footer. (3) **Staged-Projects clarification** added to NSW T8 commitmentEligibility detail: Stage 1 CIS + Stage 2 LTESA workable IF Stage 2 is genuinely distinct (own connection point, own planning, own SPV, independent economics) — captured via federal CIS T8 EC11 "Existing Project (if it is a Staged Project)" reference + NSW T8 EC10 single-Project rule; legal review of the proforma "Project" definition required. (4) PPA Scenarios block heading re-labelled from "THE UNRESOLVED QUESTION" (v3.15.0 framing when PPA-NOR mechanic was refuted in adversarial verification) to "HOW PPA REVENUE ENTERS THE LTESA CASH FLOW" reflecting the v3.15.2 confirmed mechanic from primary docs. tsc + build + browser-verified (both modals render correctly; cashflow math validated at default inputs).
- **v3.15.2** — NSW Tender 8 card rebuilt from gazetted primary documents (Tender Guidelines 20 May 2026 + MC1 Market Briefing Note 20 May 2026 + Generation LTESA Fact Sheet 15 May 2026 + Hybrid Generation LTESA Fact Sheet 15 May 2026 + proforma Generation LTESA + proforma Hybrid Generation LTESA, all supplied by Travis directly). Material corrections + previously-unresolved questions settled:

  **Settled answers to the v3.15.0 "open questions":**
  - **Hybrid LTESA price-risk share: 50% symmetric** (gazetted MC1 Briefing Appendix A formula: CostEstimate = Σ NQ × 50% × (Strike − Floating)). The asymmetric 80% downside that stakeholders pushed for during consultation was NOT adopted.
  - **Default Bid contract term: 20 years FIXED** (Alternative Bid permits varying as a Bid Variable). Confirmed gazetted — my earlier hedge to 15 yr was wrong.
  - **PPA interaction: DEFINITIVE.** PPA revenue does NOT enter the swap settlement during exercise (the swap settles on spot/Floating Price). PPAs DO reduce the Repayment obligation in non-exercise periods via the "eligible contract" reduction in the proforma LTESA. The two-branch ppaScenarios in v3.15.0 (built when this was refuted in adversarial verification) is collapsed to a single confirmed-mechanic version.
  - **Bid Variables (gazetted):** Fixed Price (Gen LTESA) / Strike Price (Hybrid), Repayment Threshold Price, COD Target Date, Excluded Periods, Contracted Percentage (partial-capacity bid e.g. 250 MW LTESA on 500 MW project), Contract Term (Default 20yr; Alternative variable). Flat nominal $ — NOT per-year schedules.
  - **Hybrid eligibility:** storage ≥4hr at First Option Date (not just COD) AND gen-export > storage-export measured at the Connection Point IN AC.
  - **Settlement basis:** Hybrid = Sent-Out Net Exports (exports minus battery imports) × Swap % × 50% × (Strike − Floating); Notional Quantity = 0 in negative-spot intervals. Generation LTESA = Sent-Out Generation × Swap % × (Fixed − Floating); Floating floored at $0/MWh.
  - **Swap Period: 1 financial year** (reduced from 2 yr in TR4), 1 July – 30 June, monthly settlement within.
  - **AC vs DC coupling:** not mandated as such — what matters is the Connection Point AC measurement for the gen-export > storage-export gate + sub-meter installation for the Generation Project. DC-coupling vs AC-coupling is the proponent's design choice (with economic implications via net-export accounting).

  **Eligibility nuance (Travis flagged "I'm sure they will have revised the date"):**
  - **EC6 hard eligibility gate: 14 November 2019 anchor UNCHANGED.** Per gazetted EC6: "Project was not identified as committed or existing in the AEMO Generation Information page published (by AEMO) on 14 November 2019". This was NOT refreshed at the eligibility-gate layer for TR8.
  - **NEW SEPARATE MC1 zeroing trigger** (the actually-refreshed anchor): the gazetted MC1 Tender Guidelines + MC1 Briefing introduce a Wholesale Market Benefits zeroing penalty for projects "In Service" in the AEMO "NEM July 2025 Generation Information" page. Applies for Generation + LDS tenders from TR8 until the next IIO Report is issued.
  - **Hybrid LTESA bids assessed as having NO Wholesale Market Benefits** if Contracted SP Storage Capacity at COD reflects less than 4 hours of storage — gazetted at Section 4.2 MC1 + Table 2 Key Items. The "zero wholesale market benefits" phrasing that was previously hedged in AURES content is CONFIRMED.

  **Other gazetted facts captured:**
  - Tender size: 7,000 GWh p.a. (~2.5 GW gen capacity). For Hybrids, only gen MW counts toward target — 100 MW solar + 100 MW BESS hybrid = 100 MW.
  - Min Project Capacity: 30 MW generation asset.
  - Merit weightings (Table 13): MC1 Financial Value & System Benefits 49% / MC2 Deliverability 17% / MC3 Org-Resource-Financing 17% / MC4 Social Value 17%.
  - Cannot hold LTESA + CISA simultaneously (EC10).
  - CWO + SW REZ Access Right holders eligible to bid TR8 even if previously awarded LTESA (carve-out in EC3, subject to Finance and Construction Criteria not yet achieved).
  - Process Security Bond: $4,000/MW capped at $1.2M, due within 8 Business Days of bid submission.
  - Tender dates: Open 20 May 2026 · Registration close 22 June 2026 5pm AEST · Bid close 6 July 2026 10am AEST · Announce Nov/Dec 2026.

  **UI changes:** productComparison reframed (Option 1 Fixed Shape consultation drop, replaced with: Gen LTESA / Hybrid Gen LTESA / Default-vs-Alternative-Bid structuring lever); ppaScenarios collapsed two-branch → single confirmed-mechanic; designStatus.decided expanded significantly; openQuestions reduced to 2 residual items (proforma "eligible contract" definition + next IIO Report timing); caveats consolidated; sources replaced with primary gazetted documents.
- **v3.15.1** — CIS Tender 8 card rebuilt from gazetted documents (Tender Guidelines Nov 2025 + MC1 Market Briefing Note Nov 2025, both supplied by Travis directly — primary sources, not trade-press paraphrase). Material corrections across all three federal CIS cards: (1) **Additionality cutoff date** moved from "23 Nov 2023 (CIS expansion announcement)" to the **round-specific AEMO Generation Information page** mechanism — CIS T8 anchors to 8 Nov 2024 (per gazetted EC7); T9 and T10 expected to follow the same pattern with their own anchor pages, currently flagged "likely" / "inferred" pending Guidelines publication. (2) **CIS national target** corrected from 32 GW to **40 GW by 2030** per the Nov 2025 Tender Guidelines (the 32 GW figure was the pre-expansion target). (3) **Minimum storage duration** for CIS T8 corrected from "4-hour equivalent" implied minimum to the actual **2 hours at COD** (the 4-hour-equivalent is a capacity-counting basis for the 16 GWh target, not a duration minimum). (4) **NSW eligibility for CIS T8** added — NSW + Victoria have RETA carve-out allocations (~0.2 GW + 0.1 GW respectively) so NSW IS eligible for T8; the NSW exclusion applies only to CIS T9 (NEM Generation). (5) **Gazetted merit-criteria weightings** added — MC1 50% / MC2-5 each 12.5%, replacing the previously-empty placeholder. (6) **CISA cashflow formula** added — `if NOR < AF: pay min(90% × (AF-NOR), APC); if NOR > AC: repay min(50% × (NOR-AC), APC); otherwise zero` — confirming 90% floor support and 50% ceiling sharing are FIXED government parameters, not bid variables. (7) **Bid variables** clarified: Final Support End Date, Annual Floor, Annual Ceiling, Annual Payment Cap — each can be a per-year schedule of nominal $ amounts (not just a flat figure). (8) **Aggregated Projects** new feature flagged for T8 — 5 MW components in same NEM region can aggregate to ≥30 MW. (9) **Hybrid T7/T8 exclusion** rule captured — Hybrid Projects participating in CIS T7 (NEM Generation) cannot also bid T8. (10) **NOR scope for MC1 assessment** narrowed: PEAR (spot market arbitrage) + FCAS only — PPA revenue NOT in modelled NOR for scoring (caveat retained for the actual proforma CISA settlement basis, which may differ). T8 playbook + analyst read + whatChanged + sources all rebuilt from primary documents. CIS T9/T10 commitmentEligibility refactored to flag the round-specific AEMO Gen Info page mechanism as the actual gating rule (Guidelines pending).
- **v3.15.0** — NSW Tender 8 strategy deep-dive on the Open Rounds tab. Backed by a deep-research workflow (105 sub-agents, ~3M tokens, adversarial 3-vote verification on 25 falsifiable claims — 19 confirmed, 6 refuted). The expanded NSW T8 card now carries six new analyst sub-sections rendered between the hybrid-mechanism callout and the Operator Playbook: (1) **Design status — decided vs open** — Option 2 (Generation-following with Price Risk Share) is the adopted Hybrid LTESA design per ASL's 17 Mar 2026 feedback summary; Option 1 (Fixed Shape/Fixed Volume) was NOT adopted; final risk-share % open (50:50 consulted, stakeholder push for asymmetric ~80% downside); AC vs DC coupling, contract term (15 vs 20 yr), and per-year/staging/partial-capacity flexibilities all flagged as still-unresolved. (2) **Product comparison** — side-by-side card for Option 1 / Option 2 / standard Generation LTESA with adopted / not-adopted / available verdict pills. (3) **Settlement basis pros & cons** — 4-quadrant callout (Pros · Cons · Favours · Disfavours) for the net-export settlement, with specific guidance per project archetype. (4) **PPA × LTESA interaction scenarios** — the headline commercial question, presented honestly as a **two-branch** analysis because the deep research refuted (0-3 then 1-2) the assumption that Net Operational Revenue captures PPA revenue; Branch A (cautious / NOR includes PPA) and Branch B (opportunistic / NOR excludes PPA) each walk through 4 scenarios (PPA below/above RRP × PPA below/above Annuity Cap), with outcome + structural trap per scenario. (5) **Hybrid-or-not decision framework** — 8 dimensions to evaluate (storage:gen ratio, duration, AC/DC coupling, PPA pre-commitments, REZ access, COD timing, curtailment, planning maturity) + two case-specific tables: **wind+BESS** (target 60–100% storage:gen ratio, ≥4h, sweet spot is 80%+ at 4–6h) and **solar+BESS** (target 80–100% ratio, ≥4h preferably 6h to capture evening peak). (6) **Open questions** — explicit list of what's NOT yet in public T8 docs (risk-share %, NOR's treatment of PPA revenue, AC/DC, contract term, staging, additionality refresh). **Corrections to v3.14.x content folded in:** softened "max 20-yr term" claim (15yr T7 carryover is the verified standard); removed "DC-coupling mandated" assertion (refuted in verification); updated "two designs consulted" framing to reflect Option 2 adoption; bumped commitment-eligibility confidence from "likely" to "confirmed for Tender Rules" with proper attribution to the May 2024 NSW Consumer Trustee Tender Rules Project Eligibility Criterion 6; expanded caveats list to 7 items reflecting the new research's confidence boundaries. Source list expanded from 4 to 12 entries including ASL primary docs (T6/T7 market briefings, 17 Mar 2026 feedback summary, May 2024 Tender Rules, 23 Mar 2026 GM speech), Dentons 26 May 2026 note, Energy Synapse developer's guide, and the May 2026 trade-press launch coverage.
- **v3.14.1** — Commitment-eligibility commentary on each open round (Travis ask: "can projects that have already taken financial close bid? Sometimes there's a data cutoff"). New `commitmentEligibility` field on the `OpenRound` type ({ summary, cutoffDate, detail, confidence }), rendered as a distinct cyan callout on every Open Rounds card with a confidence pill (confirmed/likely/inferred). Captures the additionality screen per scheme: **Federal CIS (T8/T9/T10)** — projects that reached committed status / financial close before the **23 November 2023** CIS expansion announcement are non-additional and ineligible; pre-FID projects (or FID after that date) can bid (confidence: confirmed for T9/T8, inferred for the not-yet-open T10). **NSW Roadmap LTESA (T8/T9)** — targets projects that became committed after **14 November 2019** (NSW EII Roadmap date); the LTESA is explicitly designed to help a project *reach* FID/financial close, so an already-committed merchant project isn't the target (confidence: likely — exact T8/T9 thresholds pending gazetted Guidelines). Sourced from DCCEEW + ASL tender guidance and law-firm notes. Browser-verified.
- **v3.14.0** — "Current State of Play" open-rounds tab on Scheme Intelligence. New **"Open Rounds"** tab on `/intelligence/scheme-tracker` (between CIS Briefing and the CIS/LTESA Timeline), surfacing live + imminent tenders across both the federal CIS (DCCEEW) and NSW Roadmap LTESA (ASL) programs with research-backed strategy cheat-sheets. Backed by two deep web-research passes (federal CIS + NSW LTESA) drawing on DCCEEW + ASL tender pages/market briefings, law-firm notes (HSF Kramer, Hamilton Locke, Dentons) and trade press (pv magazine, pv-tech, energy-storage.news), late May 2026. **Five rounds modelled** in a new `OPEN_ROUNDS` array + `OpenRound` type in `scheme-rounds.ts`: (1) **CIS Tender 9 — NEM Generation** (OPEN, ~5 GW, bids close 20 Jul 2026, NSW EXCLUDED, First Nations 500 MW set-aside, Victorian solar-only 470 MW cap, single-stage 5-criteria reform, commercial departures now an exclusion-risk not a scored criterion); (2) **NSW Roadmap Tender 8 — Generation + first-ever Hybrid LTESA** (OPEN, ~2.5 GW, bids close ~6 Jul 2026, merit weightings 49/17/17/17, hybrid eligibility = gen export ≥ storage export + ≥4h, cash-settled-swap-on-net-exports product, 20-yr term, DC-coupling mandated, COD-before-31-Dec-2029 favourable consideration); (3) **NSW Roadmap Tender 9 — LDS LTESA** (OPEN, ~12 GWh/~1.5 GW ≥8h, new system-strength scoring favouring PHES/A-CAES); (4) **CIS Tender 8 — NEM Dispatchable** (EVALUATING, 4 GW/16 GWh, results Jun 2026); (5) **CIS Tender 10 — NEM Dispatchable** (UPCOMING, ~Jun 2026). Each round card carries: status chip + config-favoured badge, a key-dates strip (opened/reg-close/bids-close/results), contract mechanism, an **analyst read**, a **"what changed vs the previous round"** list (tagged by Contract / Merit criteria / Eligibility / Target / Process / Region), a **merit-criteria weightings table**, a hybrid-mechanism callout where relevant, a green **"Operator Playbook"** callout (punchy imperative "how to position to win" lines — Travis's requested Option-2-in-a-callout), an amber **confidence & caveats** box (honesty rule — flags unpublished Guidelines, indicative state splits, the unconfirmed "zero wholesale benefits" phrasing), and source links. Tab also has a **Bid-Close Calendar** strip (days-left countdown, colour-coded <14d red / <35d amber) and a scheme filter (All / Federal CIS / NSW LTESA), plus a hard disambiguation note that both schemes run a "Tender 8" and "Tender 9" concurrently in 2026 — different programs. **Reconciliation fixes** (Travis flagged): softened the v3.10.3 NSW Wind "no wholesale market benefits" claim to the verifiable eligibility-gate fact (the exact penalty phrasing is unconfirmed pending gazetted Guidelines); updated the CISA-vs-LTESA comparison counterparty to "ASL — AusEnergy Services Ltd (formerly AEMO Services)". Browser-verified desktop + mobile (375px); generation-capacity stat corrected to exclude NSW T9's storage power (7.5 GW gen + ~12 GWh storage, not a conflated 9 GW).
- **v3.13.0** — REZ Pipeline intelligence surface (closes the v3.12.0 spawn-task chip). Two surfaces ship: (1) **new "REZ Pipeline" tab on `/intelligence/transmission`** with hero stats (committed/anticipated/access-rights MW · GW totals), a Recharts top-15 stacked bar chart per REZ (Committed blue + Anticipated sky + Access Rights emerald), per-REZ DataTable (drill-down to `/rez/:id` via canonical state-prefixed IDs that match `REZ_ZONES`), NSW access-scheme utilisation panel surfacing CWO 7,151 MW + South West NSW 3,560 MW EnergyCo allocation against IASR Committed/Anticipated MW per scheme with an Allocated ÷ Pipeline ratio readout (flags when EnergyCo is ahead of AEMO's classification), and an unmatched-IASR callout listing 29 IASR projects without a REZ assignment (typically gas peakers / pumped hydro / BESS at existing substations); (2) **augmented `/rez/:id` per-REZ pages** with a new "Pipeline & Access Rights" section showing AEMO 2025 IASR Committed/Anticipated MW breakdown for that zone (with AEMO REZ ID and direct IASR source link), list of IASR projects in the zone not yet matched to an AURES record (data quality signal), and the full EnergyCo Access Rights detail for the two NSW REZs that have access schemes (each access right links to its project if matched, shows technology + MW + registration date). New aggregation exporter `pipeline/exporters/export_rez_pipeline.py` joins `projects` + `aemo_iasr_projects` + `energyco_rez_access`, normalises a noisy set of REZ names into a single canonical key per zone via `REZ_CANONICAL` (handles "Central-West Orana" / "Central -West Orana" / "nsw-central-west-orana" / the "Issac" typo / "Far North QLD" merge cases). Output: `frontend/public/data/analytics/intelligence/rez-pipeline.json` (21 REZs, 11.5 GW IASR Committed across 58 projects + 8.2 GW IASR Anticipated across 32 projects + 10.7 GW EnergyCo access rights). New `fetchRezPipeline()` + `RezPipelineData` / `RezPipelineEntry` types in `dataService.ts` / `types.ts`. New `aemo_iasr` + `energyco_rez_access` `SourceId` registrations in `dataSources.ts` with refresh commands wired into the Sources Hub. New methodology guide `rez-pipeline-methodology` in `guides.ts`. Polish item flagged for v3.14: per-REZ context chips on `ProjectDetail` ("1 of 13 access rights in CWO REZ"), and a Leaflet polygon view of REZ boundaries if AEMO ISP GIS data is tractable.
- **v3.12.0** — TNSP register layer kicks off — Phase 1: EnergyCo NSW REZ Access Rights + AEMO IASR Workbook. Two new importers added to the regular data refresh workflow (documented in new `docs/DATA_REFRESH.md`). (1) **EnergyCo NSW REZ Access Rights Register** (`pipeline/importers/import_energyco_rez_access.py`) — downloads the public `access-rights-register.xlsx` from EnergyCo (66 KB, 19 entries / 10.71 GW across CWO + SW REZ schemes), parses one row per access right, fuzzy-matches to projects via `PROJECT_MAP` (zero unmatched at first run), populates new `energyco_rez_access` table for traceability, aggregates per-project totals into new `projects.rez_access_status` / `rez_access_mw` / `rez_access_date` / `rez_access_scheme` columns (14 projects updated — Yanco Delta SW 1,460 MW, Liverpool Range CWO 1,332 MW, Dinawan Energy Hub SW 1,007 MW, Valley of the Winds CWO 919 MW, Pottinger SW 831 MW, etc.), and inserts a `rez_access` timeline event per access right (deduped by access_right_id). (2) **AEMO IASR Workbook** (`pipeline/importers/import_aemo_iasr.py`) — downloads the 17 MB `2025-inputs-and-assumptions-workbook.xlsx`, parses the "Existing Gen Data Summary" sheet (83 sheets total in the workbook), filters to the 146 Committed/Anticipated/Additional policy-supported projects, fuzzy-matches to DB projects (94 of 146 matched, 64%; remaining 52 mostly gas peakers / pumped hydro / stage-specific projects flagged for fuzzy-matcher refinement in v3.12.1), populates new `aemo_iasr_projects` table, and **backfills `projects.rez` for 61 projects** with no prior REZ assignment (COALESCE protected — never overwrites hand-curated values). Critical added value: the IASR exposes per-project REZ Location + REZ ID (Wagga Wagga / N5, Central-West Orana / N3) that the monthly Gen Info Excel does NOT. Top REZ exposure surfaced: Central-West Orana 2,585 MW, South West VIC 1,726 MW, Isaac 1,103 MW. **Frontend:** new `RezAccessRow` chip in `ProjectDetail.tsx` Grid Connection section showing access-rights status (granted emerald · transferred amber · terminated red), scheme label (Central-West Orana / South West REZ), MW total, and earliest awarded date. Six new fields added to `OVERLAY_OVERRIDE_FIELDS` (`rez_access_*` + `rez_access_notes`) so hand-curated TNSP refinements survive re-exports. **Runbook:** new `docs/DATA_REFRESH.md` documents the canonical refresh order (importers → validators → mega-exporter → version bump), per-importer URLs and cadence (news daily · AEMO Gen Info monthly · AEMO IASR annual · EnergyCo REZ as-updated · OE performance monthly · MMSDM monthly with 45-day lag), what each updates, and what's still manual (TNSP 5.3.4/5.3.6/GPSA stage detail — commercial-in-confidence). **Scope note:** AEMO IASR is the *real* automation target (originally I thought TNSP Connections Registers were structured public registers — they're not; they're post-construction historical lists. The IASR Workbook's REZ enrichment + per-project Committed/Anticipated detail is what closes the gap).
- **v3.11.0** — Connection-status backfill across all projects (AEMO baseline). Closes the v3.09.4 spawned follow-up. `pipeline/importers/import_aemo_gen_info.py` now derives `projects.connection_status` from the AEMO Generator Information per-DUID `Status` column. New `CONNECTION_STATUS_MAP` (In Service → operating · In Commissioning → commissioning · Committed/Committed* → committed · Anticipated → anticipated · Publicly Announced → proposed · Announced Withdrawal → null) and `CONNECTION_STATUS_PRIORITY` to resolve multi-DUID projects to their most-progressed tier. Pipeline-data-protection rule honoured: every UPDATE branch and the INSERT branch use `connection_status = COALESCE(NULLIF(connection_status, ''), ?)` so any pre-existing hand-curated value (e.g. the 12 NSW Wind cohort entries) is preserved — the importer only fills NULL/empty rows. **Coverage:** 1,051 of 1,064 projects now have a connection_status (was ~360 NSW projects empty before). The remaining 13 NULLs are all consolidated-project vestiges (Stage 2/3 entries the importer correctly skips — out-of-scope cleanup). Distribution post-backfill: proposed 699 · operating 223 · anticipated 70 · committed 48 · commissioning 11. Seven legacy values ("Connected", "Pre-application", "In progress") normalised to the new ordinal vocabulary at the DB level + 10 overlay JSON files at `data/projects/<tech>/<id>.json`. `pipeline/exporters/export_json.py:40` `OVERLAY_OVERRIDE_FIELDS` extended with `connection_status`, `connection_nsp`, `connection_notes`, `connection_date`, `aemo_connection_date`, `scheme_contracts` so hand-curated TNSP-grade overlay refinements survive re-exports. **Frontend chip:** new `ConnectionStatusRow` helper in `ProjectDetail.tsx` renders the value as a colour-coded pill (matches the v3.09.4 `CONNECTION_CONFIG` palette) — Operating green · Commissioning purple · Committed blue · Anticipated sky · Proposed slate · etc — replacing two raw-text `DetailRow`s on the Key Details + Grid Connection sections. **Scope note:** This ships the AEMO half. TNSP register imports (Transgrid Connections Register stages 5.3.4 → 5.3.6 → GPSA executed; Powerlink Connection Pipeline; ElectraNet updates) remain a future sweep that would add granularity beyond AEMO's `Publicly Announced` bucket — surfaced today on projects like Liverpool Range and Dinawan which sit at AEMO `proposed` despite holding CIS contracts (AEMO requires 5 of 6 commitment criteria including financial close, not just a scheme award).
- **v3.10.6** — Three corrections / UX adds: (1) Travis flagged that **ICA Partners is not a construction conglomerate** — they're an Australian investment partnership co-owning the 72 MW Woolsthorpe Wind Farm with Enerfin/Statkraft. Removed `ica partners` from the conglomerate regex in `companyCategoryFor`, added a new "Investment partnership" category (matches `ica partners|partners group|macquarie|infrastructure capital|first sentier`), updated the Insights callout to read "4 of 19 (Grupo Cobra Spain ×2, Gamuda Malaysia ×2)" instead of 5, and explicitly noted ICA Partners' classification. (2) Added **row-number column (1-12)** to the "NSW Wind — Yet to Win a Scheme" table. (3) Added **estimated COD column** to that table with explicit "AURES estimate" footnote marker (`*`) for projects without a DB `cod_current` value, sorted ascending. Five candidates use DB CODs (Piambong 2028-12, Bullawah S2 2029-02, Hills of Gold 2029-12, Bendenine 2030-07, Skye Ridge 2031-11, Bookham 2032-01); seven use AURES estimates derived from planning stage + typical 4-5yr build window. **Hybrid LTESA bonus highlighting**: rows with COD ≤ 31 Dec 2029 get a green background (eligible for the COD acceleration bonus); 2030 → amber; 2031+ → red. Caveat callout surfaces the critical tension: the three highest-probability hybrid winners (Pottinger, Bookham, Hargraves) are all 2031+ COD — they win on hybrid eligibility but lose the COD bonus. Only Piambong, Bullawah Stage 2 and Hills of Gold clear the Dec 2029 threshold, and all three are wind-only — losing the hybrid eligibility component. Extended `LtesaCandidate` type with optional `cod_expected` + `cod_basis: 'db' | 'aures-estimate'` fields.
- **v3.10.5** — Hybrid LTESA weightings updated to the current ASL precedent (Tender Round 6) — **MC5 Financial Value & System Benefits at 45%**, not the stale Tender 1 figure (40%). Travis correctly pushed back on v3.10.4's use of the T1 weightings: ASL has materially restructured the criteria since T1 (consolidated 8 → 7 with MC5 now combining wholesale-market benefits + system benefits — a different criterion). Re-sourced from <a href="https://asl.org.au/-/media/services/files/tender-round-6/260130-nsw-roadmap-tender-round-6-market-briefing-note.pdf">ASL's NSW Roadmap Tender Round 6 market briefing PDF</a> (Jan 2026, Appendix 3) — the most recent confirmed weightings: MC1 Impact on electricity system 11% · MC2 Pathway to commercial operation 11% · MC3 Organisational capacity 11% · MC4 Community engagement + shared benefits + land use 11% · **MC5 Financial value + system benefits 45%** · MC6 Contract departures (qualifier) · MC7 Regional economic development 11%. Updated the Hybrid LTESA callout to reference MC5/45% (instead of MC1/40%) and to correctly note that wholesale-market benefits enter MC5's BCR numerator (so failing the hybrid eligibility collapses a meaningful portion of the 45% weighting, not the 40% figure). Also corrected the COD acceleration bonus narrative to flow through MC5 (earlier revenue → higher BCR) + MC2 (Pathway, 11%) instead of the old MC1 + MC4. Footnote in the table caveats that the R8 tender pack isn't public yet — ASL may shift weightings again.
- **v3.10.4** — ASL merit-criteria weightings added to the Hybrid LTESA callout + erroneous "49%" corrected to **40%**. Travis pointed out the Hybrid LTESA section needed the overall scoring framework so the "financial-value weighting" claim could be interpreted in context. Sourced the canonical breakdown from <a href="https://asl.org.au/-/media/services/files/publications/market-briefing-series/231020-tender-1-outcomes-market-briefing-note.pdf">ASL's Tender Round 1 outcomes market briefing PDF</a> (Oct 2023, extracted via `pdftotext`): MC1 Financial Value 40% · MC2 Commercial Departures 6% · MC3 Impact on Electricity System 9% · MC4 Pathway to Commercial Operation 9% · MC5 Organisational Capacity 9% · MC6 Land Use 9% · MC7 Community Engagement & Benefits 9% · MC8 Regional Economic Development 9% (sums to 100%). MC1 is the primary statutory consideration per EII Act s48(2). Embedded an 8-row weightings table inside the Hybrid LTESA callout with "what it tests" descriptions, plus a caveat that ASL may adjust weightings round-to-round (Round 8 tender pack not yet public). Also corrected an upstream error: previous v3.10.3 cited the financial-value weighting as 49% (from a PV Tech secondary source) — the canonical ASL document confirms 40%, so updated the "Net effect" narrative to "a meaningful portion of the 40% weighting evaporates" instead.
- **v3.10.3** — Scheme-contract coverage sweep + NSW-quota quote + LTESA R8 deep-dive. (1) Coverage audit identified 75 CIS/LTESA winners that were missing `scheme_contracts` entries on their overlay files (most CIS T1, T3, T4 plus LTESA R1-R6 winners had structured data only in `scheme-rounds.ts` but not on individual project cards). Built a Python parser that extracts every project_id from `CIS_PROJECTS` + `LTESA_PROJECTS`, derives the appropriate scheme + round + capacity + storage + source URL, and batch-appends to each overlay's `scheme_contracts` array — 73 overlays updated + 2 new overlays created (Phoenix Pumped Hydro, Tomago Battery). Dedupe pass removed 4 duplicate entries where the batch's generic round label collided with hand-curated stage-specific labels. Every CIS/LTESA winner now shows its scheme contract on its project detail page. (2) Added an explicit **"NSW is now closed for federal CIS generation"** callout to the All-19-Winners analysis section with the verbatim Giles Parkinson / RenewEconomy quote (2026-05-23): *"The next tender will exclude NSW, because it has already met its agreed quota, and has just launched its own tender seeking 2.5 GW of new capacity, with a special emphasis on wind and solar hybrids that can deliver power outside of daylight hours."* — full attribution + clickable source citation. (3) Expanded the upcoming NSW LTESA Generation section ("Round 8") with a size-vs-previous-rounds table: R1 2023 950 MW target / 1,445 MW awarded (152% take-up), R3 2023 1,200 / 1,274 (106%), R4 2024 1,150 / 317 (28% — undersubscribed), R8 2026 **2,500 MW target = 2.2× R4 · NSW's biggest generation tender ever**. (4) Added a dedicated **Hybrid LTESA mechanism** callout with three explicit favouring rules: eligibility ("generation export capacity matches or exceeds storage export capacity, with storage systems providing a minimum 4-hour duration"), hard-zero penalty for ineligible configurations ("projects bidding for a Hybrid LTESA will be assessed as having no wholesale market benefits if the initial storage duration is less than 4 hours or the capacity of the generation component is below the capacity of the storage component" — direct 0 on the 49% financial-value weighting), and COD acceleration bonus (favourable consideration for ≤ 31 Dec 2029 COD — Eraring-timing aligned). Net effect described: wind-only and solar-only bids face a structural penalty on the dominant scoring axis, materially advantaging wind+BESS (Pottinger, Bookham) and tri-tech (Hargraves) candidates vs Liverpool Range Stage 2 and The Plains. Citations to PV Tech and RenewEconomy throughout.
- **v3.10.2** — CIS Tender 7 full 19-winner refresh + analysis section. The DCCEEW media release dropped 2026-05-24 with all 19 winners' names + capacities. Refreshed `CIS_PROJECTS['cis-tender-7-nem-gen']` and `ESG_TRACKER_PROJECTS` to the canonical list: 4 newly-disclosed projects added (Gundary Solar Hybrid 320+1,391 MWh Lightsource bp NSW; Gunning Solar Farm Hybrid 290+542 MWh Zero-E/Grupo Cobra NSW; Wattle Creek Solar Hybrid 180+720 MWh Spark NSW; Banana Range Wind 228 MW EDF QLD; Moranbah Solar 171+100 MWh Zero-E QLD), and 7 existing entries refreshed with corrected DCCEEW capacities (Yanco Delta 1,450 → 1,498 MW · Bullewah 283 → 300 MW · Theodore 1,000 → 1,022 MW · Bullyard 0 → 97 MW · Whyte Yarcowie 0 → 289 MW · Cellars Hill 0 → 341 MW · Weasel 0 → 200 MW). NSW count now 9 (not 8) — Wattle Creek added late, 1 over the original quota. Round total: 7.83 GW · 7.89 GWh storage. Overlay files for Gundary, Wattle Creek, Banana Range, Bullyard, Willatook, Bungaban, Theodore, Woolsthorpe, Whyte Yarcowie, Weasel updated with structured `scheme_contracts` + `timeline_events` citing the DCCEEW media release as Tier 1 source. New **"CIS T7 — All 19 Winners (NEM-wide)"** analysis section on the NSW Wind tab (between T7 NSW snapshot and the cohort table) — 4 categorisation cards (by state · by asset type · by project size · by company type) with horizontal capacity bars, plus an Insights callout: NSW dominated 9 of 19 (Wattle Creek over quota), 3 mega-projects carry 47% of capacity (Yanco Delta + Bungaban + Theodore), construction conglomerates took 5 of 19 winners (Grupo Cobra ×2, Gamuda ×2, ICA Partners) — notable foreign-EPC presence, planning execution risk concentrated (only Yanco Delta EPBC-approved among the 19), hybrid skew toward solar (6 of 8 hybrids are solar+BESS, only 2 wind+BESS). Shareable commentary refreshed with state-by-state breakdown. Both version files in sync.
- **v3.10.1** — Data refresh + exporter regenerate. Refreshed four primary data sources without overwriting any curated content: `news_articles` 255 → 340 articles (RenewEconomy + PV Magazine + Energy Storage News, latest 2026-05-25 today), `aemo_generation_info` 9,275 → 10,596 DUIDs (monthly cadence — Travis's "Never downgrade status" guard protected 568 enriched projects), `performance_monthly` Jan-Apr 2026 refreshed with cleaner OpenElectricity settled data for 224 projects, `battery_daily_scada` 565 → 640 rows (last 30 days through 2026-05-24). Status validator clean (0 mismatches across 1,058 projects scanned — just 7 informational COD-review heads-ups for manual decisions on near-COD construction projects). Mega-exporter re-ran cleanly: 47 frontend JSONs regenerated, White Rock's v2.84/v2.85 fact-correction narrative durable through the run (1,049-char `notable` + 3 suppliers + 3 ownership history + 6 timeline events + 3 stakeholder issues), all 12 NSW Wind cohort `scheme_contracts` preserved on their project cards, Mansfield Regional Pipeline export unchanged at 17 projects · 4.3 GW. Two refreshes blocked by AEMO MMSDM publication lag (~45 days): `demand_daily` and `dispatch_availability` (coal) — May archive expected mid-July, Apr archive mid-June. No new automated importer for `market_prices` or `aemo_isp_rez` (still "never" — backlog).
- **v3.10.0** — Mansfield Regional Pipeline page. New intelligence sub-page at `/intelligence/mansfield-pipeline` — proof-of-concept regional view that the eventual NEM-wide "Under Construction" map will be built on. Filters all renewable projects to within 100 km haversine of Mansfield, VIC (-37.0556°, 146.0823°) — 17 projects · 4.3 GW total at launch (7 operating · 7 planning_submitted · 2 connection_submitted · 1 construction). Page features: CartoDB Dark map with `CircleMarker` per project (radius by capacity, stroke colour by technology, fill colour by pipeline tier) + faint 100 km radius ring; multi-select chip filters for technology (Wind / Solar / BESS / Hybrid / Pumped Hydro / Offshore) and 7-tier pipeline stage; hero stat row (projects · operating MW · construction MW · pipeline MW); preset buttons (Built/building, Pipeline only, All, Clear); project list table with sortable columns + distance-from-Mansfield + AEMO status; data-coverage callout. New 7-tier `PipelineTier` enum + `PIPELINE_TIER_CONFIG` colour map in `frontend/src/lib/types.ts` (Operating → Construction → Connection Approved (AEMO Committed) → Connection Submitted (AEMO Anticipated) → Planning Approved (EPBC) → Planning Submitted → Early Stage — uses same colour palette as v3.09.4 Connection chips for visual consistency). New `pipeline/exporters/export_regional_pipeline.py` joins `projects` + `aemo_generation_info`, aggregates AEMO status across DUIDs (most-progressed wins), derives pipeline_tier, haversine-filters to 100 km, outputs `frontend/public/data/analytics/intelligence/mansfield-pipeline.json`. Hand-curated coords added (DB + overlay audit trail) for 9 known Mansfield-area dev-stage projects that had no lat/lng: Strathbogie Ranges Wind 800 MW, Glenrowan BESS 450, Glenrowan BESS Storage 400, Goorambat BESS 400, Winton Energy Reserve 1 400, Mansfield Solar 350, West Mokoan Solar+BESS 300, Winton BESS 120, Winton North Solar 100 — ~10 km placename-centroid precision, sufficient for a 100-km regional view but noted as a coverage caveat. New methodology guide `mansfield-regional-pipeline` in `guides.ts`. CENTER / RADIUS_KM constants in the exporter are the only knobs that change for the eventual NEM-wide expansion (replace static centre with a search box, allow user to pick radius). Hub card with map emoji (🗺️) under existing geographic features.
- **v3.09.4** — NSW Wind cohort table gets a Connection column. Adds `connection_status` (8-state enum: Operating · Commissioning · Committed · GPS / pre-FID · Anticipated · Connection enquiry · Proposed · At risk) and `connection_notes` (project-specific TransGrid / EnergyCo detail) to `NSWWindCohortEntry`. Data sourced from AEMO Generator Information's per-DUID `status` field (normalised) plus public TransGrid / EnergyCo / proponent updates. Highlights surfaced: Yanco Delta on the **GPS / pre-FID** pathway (1,460 MW SW REZ access right secured Apr 2025, connecting to TransGrid's Dinawan Substation, GPS assessment with TransGrid + AEMO underway, FID mid-FY27); Junction Rivers flagged **At risk** (lost SW REZ access, no connection path until recovered); Coppabella **Anticipated** by AEMO (DUID COPBEL allocated, 290 MW) despite LTESA R1 being stalled; Uungula **Committed** (under construction, AEMO confirmed); Flyers Creek **Operating** (DUID FLYCRKWF, 145.5 MW). Hover the chip for the per-project rationale. Spawned follow-up task: backfill `projects.connection_status` from AEMO Gen Info for all 1,064 projects via pipeline change (currently only 4 NSW projects have a value in the DB column), and add `connection_status` + `connection_nsp` to `OVERLAY_OVERRIDE_FIELDS` so hand-curated detail survives re-exports.
- **v3.09.3** — NSW Wind Build Timeline gets a 3-mode anchor toggle. Travis asked to be able to see the cohort organised by FID date and COD date as well as the scheme-award default. Implemented via three buttons: **Scheme Award → COD** (current default — full underwriting window), **FID → COD** (construction window only — projects without a confirmed or estimable FID drop out with an inline "FID pending — COD target YYYY" annotation), and **COD delivery wave** (narrow 1-year markers centred on expected COD year — shows the delivery cohort timing). Extended `NSWWindCohortEntry` with structured `fid_year` + `fid_status: 'reached' | 'expected' | 'pending'` fields. Confirmed FID dates added for Uungula (2024 reached — construction) and Flyers Creek (2021 reached — operating). AURES-estimated FID dates added for the EPBC-approved CIS / LTESA winners on credible delivery paths: Yanco Delta (2027 expected — Origin's mid-FY27 guidance), Spicers Creek (2026 expected), Liverpool Range Stage 1 (2027 expected), Thunderbolt Stage 1 (2026 expected). All other entries marked `'pending'` — the chart honestly excludes them from FID mode rather than fabricating a date. Caption notes the AURES-estimated vs confirmed distinction. Heading title updates dynamically per anchor mode.
- **v3.09.2** — NSW Wind tab UX fixes. (1) Snapshot card headline changed from "NSW wind awarded today" to "NSW wind awarded — 23 May 2026" so the figure dates correctly as the news ages. (2) Build Timeline rebuilt: the Recharts vertical BarChart was rendering at 0×0 inside the conditionally-mounted tab (classic Recharts measurement bug with `layout="vertical"` + late visibility) — replaced with a div-based Gantt using CSS percentages keyed off TIMELINE_START_YEAR / TIMELINE_END_YEAR constants. Rendering is now layout-reliable. (3) Added interactive project selection per Travis's "let user build up over time" UX: 12 per-project toggle chips, 4 preset buttons (All 12, CIS T7 wins, Built or building, Stalled), Clear button, live count + MW total in the toolbar. Default state is the 3 CIS T7 winners (the most newsworthy subset). Empty state shows a friendly prompt. Today (May 2026) + Eraring shutdown (Aug 2027) reference lines preserved as vertical overlays.
- **v3.09.1** — Cleanup release. Restored White Rock Wind Farm Stage 1's v2.84/v2.85 fact-correction narrative — the curated `notable`, `capex_aud_m`, `stakeholder_issues`, ownership history, suppliers, and timeline events had been silently stripped from `frontend/public/data/projects/wind/white-rock-wind-farm-stage-1.json` when the v3.09.0 exporter run regenerated the file (the original work landed directly in the published JSON, bypassing both DB and overlay). Recovery split the content along the merge-logic lines: DB rows for projects (`rez`/`lga`/`connection_status`/`connection_nsp`/`current_operator`), `suppliers` (3 rows with quantity 70 + full GW121/2.5 MW PMDD model name + Goldwind EPC), `ownership_history` (3 rows), `cod_history` (1 row), `multi_source_values` cost row, and 6 rich `timeline_events`; overlay (`data/projects/wind/white-rock-wind-farm-stage-1.json`) for `OVERLAY_OVERRIDE_FIELDS` content — `notable` (1,049 chars), `stakeholder_issues` (3), `sources` (3), `capex_aud_m`/`capex_source`/`capex_year`, `data_confidence: medium`, `confidence_score: 75`. Recovery is durable — verified semantic equivalence with v2.85.0 export. Audited 18 other NSW operating wind farms (Sapphire, Bango, Capital, Collector, Boco Rock, Bodangora, Crookwell 2/3, Crudine Ridge, Cullerin Range, Gullen Range/2, Gunning, Rye Park, Silverton, Taralga, Woodlawn) — all zero-diff, no other regressions. Also bundles the v3.09.0 exporter-output tail: refreshed analytics files (contractor-analytics, developer-analytics, project-timeline, wind-resource, grid-connection, etc. — all net-add reflecting the T7 announcement, no content strips), indexes (cod-drift, contractor-profiles, oem-profiles), and metadata. Follow-up worth considering (deferred): adding `ensure_ascii=False` to `pipeline/exporters/export_json.py` to eliminate Unicode-escape diff noise across all project JSONs going forward.
- **v3.09.0** — NSW Wind & CIS/LTESA Deep Dive + CIS Tender 7 refresh. Lands the just-announced CIS Tender 7 results (2026-05-23 — 19 projects / 7.8 GW vs 5 GW target, wind-dominated 10/9 with 8 of 19 winners being battery-paired hybrids) across `scheme-rounds.ts` (CIS_PROJECTS + new T7 round metadata), `esg-tracker-data.ts` (15 named winners + ROUND_ESG_SUMMARIES T7 row + LTESA R7), `scheme-round-info.ts` (new T7 row with state breakdown + NSW T9 exclusion call-out), and `CISLTESAModule.tsx` learning module (T7 result + NSW state pivot). New **9th tab "NSW Wind"** on `/intelligence/scheme-tracker` with 8 sections: T7 NSW snapshot (3 wind winners + 1 solar hybrid), full 12-project cohort table with award MW / total MW / staged + REZ-access detail, build timeline Gantt with Eraring shutdown reference line, "haven't won yet" candidate table headed by Pottinger and Liverpool Range Stage 2, Q2 2026 NSW Roadmap LTESA Generation tender briefing (2.5 GW, hybrid-favoured: battery ≤ gen capacity, min 4hr), 3-band qualitative H/M/L probability scoring across 12 candidates (Pottinger / Bookham / Hargraves = High; Liverpool Range Stage 2 / Bullawah Stage 2 / The Plains / Skye Ridge / Lake Victoria / Winterbourne = Medium; Bendenine / Hills of Gold / Piambong = Low), full LTESA-vs-CISA mechanism comparison table + per-scheme pros/cons, and a shareable plain-English commentary card with copy + PDF export (reuses `exportElementToPdf` tuning). New `LTESA_R8_CANDIDATES` + `NSW_WIND_COHORT` arrays in `scheme-rounds.ts` as single source of truth. New "Scheme Contracts" section on `ProjectDetail.tsx` rendering `Project.scheme_contracts` (field existed at type level but was never displayed). 12 NSW wind overlays updated with structured `scheme_contracts` + `timeline_events` (CIS / LTESA award entries) + audit-trail `stakeholder_issues` for the Valley of the Winds / Thunderbolt / Bullewah capacity-vs-spelling discrepancies (White Rock pattern). New methodology guide `nsw-wind-cis-ltesa` in `guides.ts`. Sources: RenewEconomy 2026-05-23 (Tier 2 — 15 of 19 winners named; 4 await DCCEEW publication), ASL LTESA R7 (Tier 1), DCCEEW T9 NSW exclusion guidance (Tier 1, pending full publication). SchemeBoardroom narrative refreshed (lines 112–113 + new T7 milestone in OutlookPanel + updated grid-connection constraint copy to cover T7).
- **v3.08.0** — PWA refresh-prompt fix + expanded SESSION_OPENER. `frontend/public/data/metadata/version.json` had drifted to 2.99.0 (May 13) while `package.json` was at 3.07.1 — `useVersion` polls the JSON every 5 min and surfaces a "Refresh" badge when the live version is ahead of the user's loaded version, so the drift meant PWA users on cached builds were not prompted to update for v3.00.0 → v3.07.1 (8 versions including all the Commissioning Ramp work). Both files are now bumped in sync and the release workflow explicitly requires both. `docs/SESSION_OPENER.md` expanded with: the version.json sync rule + PWA mechanism explainer, `gh run watch` to verify deploys, key-files cheat sheet (`WindValueAnalysis`, `ValuePdfSections`, `exportPdf`, `useVersion`, etc.), React-inside-React anti-pattern, JSX apostrophe-escaping rule, PDF tuning specifics (JPEG 0.82 / scale 1.5 / jsPDF compress / 44MB→1.5MB), partial-month detection algorithm, factual-honesty guidance ("hedge or attribute, don't fabricate"), White Rock correction pattern (record fixes in `stakeholder_issues`), and explicit "no emojis unless asked" rule.
- **v3.07.1** — Censoring-flag fix on Commissioning Ramp: extend `pre-2021-cutoff` to also flag assets where `cod_declared` predates `first_generation_date` by more than 60 days. Previously only assets whose first AEMO dispatch was on/before 2021-01-01 were censored, missing 12 wind farms (Wattle Point, Lake Bonney 1, Canunda, Cathedral Rocks, Mt Millar, Starfish Hill, etc.) that commissioned in 2003-2018 but whose first "generation" in the data is just where the AEMO daily snapshot starts. Their lifetime CF baseline reflects today's heavy SA curtailment, while their 2021-2022 "ramp" months reflect the pre-curtailment era — making the ratio spuriously exceed 100%. With the fix, the censored count rises from 94 → 106 and the remaining >100% wind-month outliers (Hawkesdale, Mortlake, Stockyard Hill etc.) are all genuine seasonal variation in newly-commissioned farms.
- **v3.07.0** — Ramp curves over time: new line-chart sub-section on the Commissioning Ramp tab plotting every filtered asset's monthly trajectory aligned at **month 0 = first AEMO generation**. One thin tech-coloured line per asset (capped at 40 most-recent if the filter yields more) plus two configurable overlay lines: a bold gold **group average** (computed across the current filter, label reflects the selected states/years/tech e.g. "NSW · 2025, n=1") and a dashed white **fleet average** (across all assets of the selected tech, or all 151 assets when no tech filter). Y-axis toggle between "CF as % of asset lifetime CF" (default, with 100% reference line = stable), absolute monthly CF, or cumulative energy as % of ramp-window total. Custom tooltip shows averages first, then the top 5 assets at the hovered month. Lets you literally see whether the 2024+2025 cohort is ramping faster or slower than the historical fleet, and where any specific filter (e.g. NSW solar 2025) sits relative to both.
- **v3.06.0** — Commissioning Ramp tracker: new "Commissioning Ramp" tab on `/intelligence/revenue-intel` surfaces the previously-invisible window between an asset's first AEMO dispatch and stable full output. For 151 operating solar/wind/hybrid projects: per-asset first-generation date (from `generation_daily`), stable-output date (first month at ≥80% of asset's own lifetime median monthly CF), ramp duration (days), early-period energy MWh, and early-period revenue. Revenue is **mixed-basis** — settled `performance_monthly.revenue_aud` where the OpenElectricity importer captured it (post-`status='operating'`), modelled (`gen_mwh × dispatch_price_daily.avg_rrp` for the asset's region) for the commissioning-phase months the importer skips. Each row chips its data-quality flags (`pre-2021-cutoff`, `short-history`, `no-settled-revenue`, `not-yet-stable`, `no-cod`). Tech / multi-year / multi-state filters drive a median bar chart and a sortable DataTable; clicking an asset opens a `DrillPanel` with the monthly ramp curve (settled vs modelled revenue bars + CF-as-%-of-lifetime line). New `CommissioningRampCard` on the project Performance tab for matching assets, deep-linking back to the tab via `?section=commissioning&project={id}`. New `pipeline/exporters/export_commissioning_ramp.py` standalone exporter. BESS deliberately excluded — different ramp semantics. Full data-quality narrative (definitions, source confidence, known gaps) lives in a collapsible explainer at the top of the tab.
- **v2.33.0** — BESS Records Leaderboard: new `/intelligence/bess-records` page tracking all-time discharge and charge records across 30 grid-scale NEM batteries. Per-battery peak single-day discharge/charge (MWh) ranked by battery, state (NSW/VIC/QLD/SA), and NEM. Fleet-level records: peak fleet discharge/charge day (9,913 MWh on 30 Mar 2026) and 5-min peak output (3,675 MW). Hero cards for NEM all-time records; scope pills for state filtering; dual leaderboard (discharge/charge tabs); full sortable battery table with region chips, bar fills, and peak dates; methodology note explaining data lineage. New `export_bess_records_leaderboard()` Python function joining `generation_daily` + `aemo_generation_info` + `battery_records`. New `fetchBessRecordsLeaderboard()` in dataService.ts. All data from existing MMSDM DISPATCHLOAD pipeline — no new importer required.
- **v2.32.0** — BESS Bidding Trading Platform deep-dives. Expanded the Trading Platforms section of `/intelligence/bess-bidding` with per-platform callout notes covering: OEM/hardware match (Tesla Autobidder confirmed Tesla Megapack; Neoen/Shell confirmed all-Tesla fleet; Wartsila Quantum confirmed at Eraring; Fluence hardware-agnostic), software lock-in analysis (AGL vertically integrated; Timestamp-P purchasable SaaS; Wartsila/NS Energy shows tightest OEM-operator bundling), platform convergence data (rebid rates converge 91–99% within platforms; target spreads do NOT — platform enforces strategy type, not price points; Origin/NS Energy exception with 17 identical bid days proving centralised dispatch), and grid implications per platform. New amber "Grid-scale implications" callout card at section bottom addressing: whether same-platform assets chase the same spike (same strategy, not same price, except NS Energy); VIC Fluence/MONDO concentration risk (Hazelwood 150 MW + Pine Lodge 250 MW + LaTrobe Valley on same AI forecast); NSW platform diversity as resilience (Waratah Fluence vs Eraring Wartsila/NS Energy); and the monoculture risk as Supernode (1,300 MW) and further Waratah stages scale. New `PlatformNote` helper component.
- **v2.31.1** — Quick-win data + peak-calc patch. Refreshed `aemo_generation_info` (1,064 projects) and `news_rss` (85 new / 255 total articles). Imported 2026-03 `generation_daily` (6,432 rows). Fixed the Energy Transition Scoreboard NEM demand peak: previously the sum-of-regional-peaks (34,515 MW) overstated the true concurrent NEM peak because regions don't reach individual peaks at exactly the same 5-min interval. `import_dispatch_regionsum.py` now writes a 'NEM' pseudo-region row per date with `peak_demand_mw = max over intervals of SUM(TOTALDEMAND across regions)`. New NEM peak: **33,674 MW on 16 Dec 2024** (2.5% correction). NEXT_SESSION_HANDOFF.md rewritten to reflect v2.31.0 state.
- **v2.31.0** — CIS Wind Pipeline filter: closes the last item from the original v2.29.0 bundle. Extends `/intelligence/scheme-tracker` → Milestone Tracker with a technology filter (Wind / Solar / BESS / Hybrid / VPP / Pumped Hydro), a **🌬️ CIS Wind (NSW)** preset, and NSW seeded as the default state. New **Dev Status** column on every expanded round's project table — colour-coded chip surfacing auto-computed "why not building" annotations (pre-planning / grid connection pending / environmental pending / execution risk) plus precedence for FID reached / construction / planning approved / operating / commissioning. Exporter change: `export_scheme_tracker()` now joins `eis_technical_specs` + `developer-scores.json` + `projects.development_stage` + `projects.connection_status` to compute the annotations. Developer grades also surface inline on the table.
- **v2.30.0** — Energy Transition Scoreboard: new `Transition Scoreboard` tab on `/intelligence/energy-mix` stacks coal YoY decline against wind / solar / BESS discharge growth with total demand overlaid as a dashed line, at NEM + 5 state scopes. Two new importers (`import_generation_daily.py` re-parses cached MMSDM zips with an expanded DUID filter for solar/wind/BESS; `import_dispatch_regionsum.py` pulls DISPATCHREGIONSUM for daily MWh + peak MW per region). New `export_energy_transition()` joins coal + tech + demand into a YTD + full-year JSON, plus a transition-specific records board (coal lows + renewable highs + demand peaks per scope). Also fixed a DISPATCHLOAD revision-dedupe bug where intervention-case rows inflated non-coal totals by 2-6× (coal was unaffected thanks to the UNIQUE(settlement_date,duid) constraint). Guides now show `added` / `updated` dates with "New" / "Updated" badges; new guide for the scoreboard, YTD addendum on the Coal Outage guide. Tab-bar scrollbar also no longer overlaps content below with 7 tabs. NEM headline: coal -10%, wind +56%, solar +126%, demand flat across YTD 2021→2025.
- **v2.29.1** — Coal YTD refresh with 5-year history: ran the MMSDM backfill (`pipeline/scripts/backfill_coal_history.sh`) across 2021-2025, landing ~24M 5-min coal dispatch rows. Re-exported `coal-ytd-comparison.json`. YTD Comparison tab now shows real prior-year rows with NEM CF 67.5% (2021) → 60.5% (2026). Also fixed `import_dispatchload.py` to handle the new `PUBLIC_ARCHIVE#DISPATCHLOAD#FILE01#...` filename format (Aug 2024+) while keeping the legacy format as fallback.
- **v2.29.0** — Coal Watch YTD + Same-Period Comparison: new "YTD Comparison" tab on Coal Watch resolves the apples-to-oranges problem (previous Eraring hero compared partial-2026 GWh to full-2025). Aggregates `dispatch_availability` into per-(year, day-of-year, DUID) MWh and rolls up at NEM / state / station level with two windows — YTD (Jan 1 → cutoff doy) and full calendar year. Scope pills (NEM / NSW / QLD / VIC), year pills (all years present), window toggle (auto-defaults to YTD when the in-progress year is selected, Full-year for historicals). Graceful degradation: renders whatever years are present, surfaces `days_covered` so partial coverage is honest. MMSDM backfill to extend prior-year history remains a follow-up release. New `export_coal_ytd_comparison()` in pipeline.
- **v2.28.0** — NEM Live Market Pulse Phase 2: Battery Records + Live Activity. New Live & Records tab on `/intelligence/bess-portfolio` surfacing all-time fleet records (max 5-min discharge/charge, max daily discharge/charge) per region (NEM + 5 states) plus latest-day snapshot and 30-day throughput chart. New `import_battery_scada.py` importer pulls 5-min NEM power from OpenElectricity's `/data/network/NEM` endpoint (one API request per month of history — cheap). New daily aggregation schema (`battery_daily_scada`, `battery_records`) stores no raw 5-min data — just per-region daily rollups and all-time records. Graceful pending-state rendering until importer runs (same pattern as v2.27.0). New `openelectricity_5min` DataProvenance source and methodology guide.
- **v2.27.0** — NEM Live Market Pulse Phase 1: Coal Outage vs Dispatch Erosion. Distinguishes coal MWh reductions into outage-driven (unit unavailable) vs market-displaced (unit offered but not dispatched) — the difference between mechanical events and structural decline. New NEM-wide coal config (15 stations, 44 DUIDs, 21.2 GW NSW/QLD/VIC). New `import_dispatchload.py` importer pulling AEMO NEMWEB DISPATCHLOAD data. New `export_coal_outage_dispatch()` producing NEM / state / station aggregates. New "Outage vs Dispatch" tab on Coal Watch with classification rules, per-state breakdown, and per-station table. Framework renders gracefully before DISPATCHLOAD data is populated; running the importer brings the percentages to life. First of three planned "NEM Live Market Pulse" enhancements integrated into existing intelligence pages (from the phone-brief integration plan).
- **v2.26.0** — T3.I Supply Chain Concentration Risk + T3.J Scheme Win Probability (bundled). New `/intelligence/risk-signals` page with 5 tabs. **T3.I** surfaces 11 dominance cells (where 1 OEM holds ≥40% of installed MW in a tech×state×role cohort): 3 monopolies (Vestas TAS wind 100%, EKS Energy NSW BESS inverter 77%, SMA VIC solar inverter 76%) + 5 dominant + 3 concentrated. At-risk OEM exposure: Senvion 5 projects / 741 MW, Suzlon 7 projects / 697 MW, Acciona Windpower 1 project / 192 MW. Dev-chain risks: Equis × Tesla and Lake Bonney × Vestas both at 100% single-OEM dependency. **T3.J** ranks 782 development-stage projects with a heuristic composite (developer grade + tech fit + size fit + readiness + repeat-winner bonus); 2 projects in high-probability band (Haughton BESS, Karara BESS — both QLD BESS with grade-A developers). Completes the Tier-3 feature track.
- **v2.25.0** — T3.H + T3.K Asset Lifecycle & Repowering (combined). New `/intelligence/asset-lifecycle` page with 6 tabs (Overview · Age Distribution · Refurb Candidates · OEM Fleet Ages · Fleet Turnover Forecast · Historic Deals). Scores 74 repowering candidates using age (0-60) + at-risk-OEM flag (0-25, covering Senvion/Suzlon/Acciona-Windpower/REpower + Vestas legacy V66-V90) + CF underperformance (0-15). Top candidates: Woolnorth (25y Vestas, score 90), Hallett Stage 1 Suzlon fleet (SA, scores 76-86), Portland Wind Farm (VIC, Senvion). 59 projects ≥15 years old, 42 ≥20 years. 2026 turnover forecast = 4 projects / 1 GW (mostly hydro refurb), ramping through 2029-2036. New `export_asset_lifecycle()` in pipeline + methodology guide.
- **v2.24.0** — T2.F Solar Resource + T2.G BESS Portfolio Intelligence (bundled). **Solar Resource** at `/intelligence/solar-resource` mirrors Wind Resource with solar-specific CF thresholds (≥29% Excellent) · state/REZ/capacity-class benchmarks · developer fleet benchmarks (placeholder — no developer yet has ≥3 operating solar) · 150 development pipeline predictions. New Capacity Class chart surfaces the "larger plants, lower CF" pattern (inverter clipping + network curtailment). **BESS Portfolio Intelligence** at `/intelligence/bess-portfolio` is a new 6-tab page (Overview · Duration · Grid-Forming · Co-located · Chemistry · Network Services). Shows 2017→2029 duration evolution (0.89h → 4.07h avg), 11 grid-forming projects led by Eraring 700MW/4.5h, 92 co-located hybrid projects, EIS-verified chemistry (33 LFP vs 1 NMC), 4 documented network-service contracts. New `export_solar_resource()` and `export_bess_portfolio()` in pipeline. Two new methodology guides. **Completes the Tier-2 feature track** — the core intelligence layer + Tier-1 + Tier-2 are all now shipped.
- **v2.23.0** — T2.D Contractor Intelligence: `/contractors` rebuilt as a 5-tab intelligence page (Overview · EPC · BoP · Developer × Contractor · Directory) · ContractorDetail enriched with summary pills, Go-to OEM Partners table, Top Clients table, and full Project Portfolio DataTable with CSV export. New `export_contractor_analytics()` pre-aggregates concentration per role (EPC HHI 389 = highly fragmented; BoP HHI 1417 = moderately concentrated), 125 developer × contractor pairings, 119 contractor × OEM co-occurrence pairings. Surfaces the CPP × Tesla BESS-integrator dominance and Akaysha Energy × CPP as the strongest dev-contractor relationship. New Contractor Intelligence methodology guide.
- **v2.22.0** — T1.C Lifecycle Quartile Matrix: new `/intelligence/lifecycle-quartile` page with state-of-the-nation grid (6 techs × 6 states × 3 stages). Each of 63 cells shows project count, MW, and a Q1-Q4 coloured strip — cells click through to DrillPanel with full project list, stage-specific columns (CF/rev for operating, drift/grade for construction, scheme/EIS/REZ for development). Three distinct scoring systems per stage: league composite for operating (220 projects), delivery risk for construction (56), readiness score for development (782). New `export_lifecycle_quartile()` pre-aggregates the matrix + per-project scores + drill IDs. New "Lifecycle Quartile Matrix" methodology guide. This completes **all three Tier-1 features (T1.A, T1.B, T1.C)** plus T2.E — the core intelligence layer rebuild is now shipped.
- **v2.21.0** — T2.E PPA Market Mapper: `/offtakers` rebuilt as a tabbed intelligence page (Overview · Top Buyers · Developer×Offtaker · Offtake Types · Uncontracted · Directory) + OfftakerDetail enriched with a full Offtake Contracts table (price, volume, tenor, confidence, clickable sources) with per-row DrillPanel. **Major data enrichment**: offtakes table went from 1/85 with MW → 57/84 with MW, 14 → 40 with tenor, 0 → 11 with published prices, 47 → 241 sources. All 7 ACT Government reverse-auction strike prices recovered ($92-$44.97/MWh). Added `export_offtake_analytics()` with buyer categorisation (gentailer/state_owned/government/corporate/industrial/trader), dev×offtaker matrix, and 142-project uncontracted-operating risk register. New "PPA Market Mapper" methodology guide.
- **v2.20.0** — T1.B Developer Portfolio Dashboard: `/developers/:slug` rebuilt from a basic stat page into an 8-section portfolio view — execution scorecard with A/B/D/F grade, scheme-win chips, pipeline waterfall, COD drift timeline, equipment preferences (go-to OEMs + contractors), conditional operating-fleet quartile distribution (9 devs qualify), offtake summary + cross-link to PPA mapper, full project list. New `export_developer_analytics()` pre-aggregates equipment + drift + scheme wins + performance + offtakes (181 devs with equipment data, 167 with performance, 16 with scheme wins, 54 with offtakes). Plan reordered to promote T2.E (Offtaker PPA Market Mapper) ahead of T1.C for v2.21.0 — offtakes needs its own first-class home.
- **v2.19.0** — T1.A OEM Intelligence deep-dive: `/oems` rebuilt as a tabbed page (Overview · Wind · Solar · BESS · Hydro · Directory) · new Hydro market-share pie · new Overview tab with HHI concentration index per role + "Top 3 per Equipment Type" + "Most Active Developer-OEM Pairings" · per-tech OEM league tables with CF% + Q1-share performance correlation · BESS tab adds $/kWh by OEM + EIS-verified chemistry callout · Hydro tab adds Fuji Tasmania monopoly callout · new `export_oem_analytics()` in pipeline pre-aggregates performance + developer cross-links (no N+1 fetches) · new "OEM & Supplier Data" guide.
- **v2.18.0** — F2 drill-down pattern: `<DrillPanel>` + `<DrillBreadcrumb>` components · click-through retrofitted on 5 charts across 4 pages (DriftAnalysis tech + state bars · Performance quartile distribution · Revenue by Tech · Revenue Pressure · BESS rebid frequency) · slide-in-right on desktop, bottom-sheet on mobile · Escape + click-outside to close · Performance methodology guide updated.
- **v2.17.1** — Refinements: Solar OEM market share chart on `/oems` (panels + inverters toggle) · BESS Capex + Project Timeline removed from hamburger (still reachable via Intelligence Hub) · **overlay merge in exporter** — manual-enrichment files at `data/projects/<tech>/<id>.json` now survive re-exports (no more copy-back needed for Waratah SIPS contracts, etc.) · Timeline ↔ NEM Activities cross-links.
- **v2.17.0** — Data provenance across the app: typed source registry + `<DataProvenance>` chip widget + 21 pages retrofitted (every intelligence page, every entity directory, BESS Capex, Project Timeline, Performance). Extended exporter registry to 11 sources (added NEMWEB bids, News RSS, AEMO ISP REZ, market prices). Each chip shows source · age · status dot; click for full description + copyable refresh command.
- **v2.16.0** — Intelligence layer foundation: ChartFrame + DataTable · solar OEMs visible (16 new, 101 total) · GridConnection merged into Transmission/REZ · analytics pages moved into /intelligence · 3-group nav (Explore · Intelligence · Resources) · stale guides retired
- **v2.15.1** — Fix stakeholder_issues rendering for object-format data
- **v2.15.0** — Solar & hydro supplier enrichment: 202 new supplier records from web research
- **v2.14.0** — Notable text enrichment: 100% coverage across all 225 operating projects
- **v2.13.0** — Real monthly performance data for 220 projects, BESS revenue, Waratah SIPS enrichment
- **v2.12.0** — Next-gen BESS tech: Quantum3 string inverters, SmartStack, Megablock, Eraring capex fix

All prior history: `git log` or GitHub Releases page.

### Known data gaps still to close (enrichment, parallel to UI work)
- 16/80 wind projects missing some supplier data
- 7/34 hydro projects missing OEM data
- Ownership history only 12 entries — could be deeper
- BESS bidding summaries could be pre-computed per project
- Web research on offtakes not yet commenced (85 known, likely many more unlisted)

---

## 8. Principles

1. **Foundation before features** — F1/F2/F3 always. Every new chart uses ChartFrame. Every new table uses DataTable.
2. **Every chart has a drill path** — aggregates without drill are dead ends for power users.
3. **Every table has row numbers + totals + sortable columns** — no exceptions.
4. **Intelligence is the centre of gravity** — operational dashboards support it, not the other way round.
5. **Leverage the enrichment** — the data is the point. If a feature doesn't require the new supplier/developer/offtake/timeline data, it's probably not intelligence, it's just reporting.
